# PackRat API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8787

# HTTP Basic Auth credentials for /api/admin/* routes
NEXT_PUBLIC_ADMIN_USERNAME=admin
NEXT_PUBLIC_ADMIN_PASSWORD=changeme
