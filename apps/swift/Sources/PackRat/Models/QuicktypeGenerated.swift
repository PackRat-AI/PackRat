// AUTO-GENERATED by `bun swift:quicktype` — do not edit by hand.
// Source: packages/schemas/src/*.ts (Zod → JSON Schema → quicktype → Swift).
//
// Wrapped in a `Quicktype` namespace so types do not collide with the parallel
// codegen paths (swift-openapi-generator emits Components.Schemas.*, and the
// legacy custom generator emits top-level structs in Models/Generated.swift).

import Foundation

public enum Quicktype {
    // This file was generated from JSON Schema using quicktype, do not modify it directly.
    // To parse the JSON, add this file to your project and do:
    //
    //   let packRatComponents = try PackRatComponents(json)

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).


    // MARK: - PackRatComponents
    public struct PackRatComponents: Codable, Equatable {
        public let catalogCatalogCategoriesResponse: [String]?
        public let catalogCatalogCompareRequest: CatalogCatalogCompareRequest?
        public let catalogCatalogETL: CatalogCatalogETL?
        public let catalogCatalogItem: CatalogCatalogItem?
        public let catalogCatalogItemsResponse: CatalogCatalogItemsResponse?
        public let catalogCreateCatalogItemRequest: CatalogCreateCatalogItemRequest?
        public let catalogErrorResponse: CatalogErrorResponse?
        public let catalogUpdateCatalogItemRequest: CatalogUpdateCatalogItemRequest?
        public let chatChatRequest: JSONAny?
        public let chatCreateReportRequest: ChatCreateReportRequest?
        public let chatUpdateReportStatusRequest: ChatUpdateReportStatusRequest?
        public let feedCreateCommentRequest: FeedCreateCommentRequest?
        public let feedCreatePostRequest: FeedCreatePostRequest?
        public let feedFeedResponse: FeedFeedResponse?
        public let guidesGuideCategoriesResponse: GuidesGuideCategoriesResponse?
        public let guidesGuideDetail: GuidesGuideDetail?
        public let guidesGuideSearchResponse: GuidesGuideSearchResponse?
        public let guidesGuidesResponse: GuidesGuidesResponse?
        public let packsAddPackItemBody: PacksAddPackItemBody?
        public let packsAnalyzeImageRequest: PacksAnalyzeImageRequest?
        public let packsCreatePackBody: PacksCreatePackBody?
        public let packsCreatePackWeightHistoryBody: PacksCreatePackWeightHistoryBody?
        public let packsErrorResponse: PacksErrorResponse?
        public let packsGapAnalysisRequest: PacksGapAnalysisRequest?
        public let packsPackItem: PacksPackItem?
        public let packsPackWithWeights: PacksPackWithWeights?
        public let packsUpdatePackItemRequest: PacksUpdatePackItemRequest?
        public let packsUpdatePackRequest: PacksUpdatePackRequest?
        public let packTemplatesAIPackAnalysis: PackTemplatesAIPackAnalysis?
        public let packTemplatesCreatePackTemplateItemRequest: PackTemplatesCreatePackTemplateItemRequest?
        public let packTemplatesCreatePackTemplateRequest: PackTemplatesCreatePackTemplateRequest?
        public let packTemplatesGenerateFromOnlineContentRequest: PackTemplatesGenerateFromOnlineContentRequest?
        public let packTemplatesUpdatePackTemplateItemRequest: PackTemplatesUpdatePackTemplateItemRequest?
        public let packTemplatesUpdatePackTemplateRequest: PackTemplatesUpdatePackTemplateRequest?
        public let passwordResetForgotPasswordRequest: PasswordResetForgotPasswordRequest?
        public let passwordResetResetPasswordRequest: PasswordResetResetPasswordRequest?
        public let seasonSuggestionsSeasonSuggestionsRequest: SeasonSuggestionsSeasonSuggestionsRequest?
        public let trailConditionsCreateTrailConditionReportRequest: TrailConditionsCreateTrailConditionReportRequest?
        public let trailConditionsUpdateTrailConditionReportRequest: TrailConditionsUpdateTrailConditionReportRequest?
        public let trailsRouteDetailRow: TrailsRouteDetailRow?
        public let trailsRouteSearchRow: TrailsRouteSearchRow?
        public let tripsCreateTripBody: TripsCreateTripBody?
        public let tripsTrip: TripsTrip?
        public let tripsUpdateTripBody: TripsUpdateTripBody?
        public let uploadPresignedUploadResponse: UploadPresignedUploadResponse?
        public let userErrorResponse: UserErrorResponse?
        public let userUpdateUserRequest: UserUpdateUserRequest?
        public let userUpdateUserResponse: UserUpdateUserResponse?
        public let userUserProfile: UserUserProfile?
        public let weatherForecastResponse: WeatherForecastResponse?
        public let wildlifeWildlifeIdentifyRequest: WildlifeWildlifeIdentifyRequest?

        public enum CodingKeys: String, CodingKey {
            case catalogCatalogCategoriesResponse = "catalog.CatalogCategoriesResponse"
            case catalogCatalogCompareRequest = "catalog.CatalogCompareRequest"
            case catalogCatalogETL = "catalog.CatalogETL"
            case catalogCatalogItem = "catalog.CatalogItem"
            case catalogCatalogItemsResponse = "catalog.CatalogItemsResponse"
            case catalogCreateCatalogItemRequest = "catalog.CreateCatalogItemRequest"
            case catalogErrorResponse = "catalog.ErrorResponse"
            case catalogUpdateCatalogItemRequest = "catalog.UpdateCatalogItemRequest"
            case chatChatRequest = "chat.ChatRequest"
            case chatCreateReportRequest = "chat.CreateReportRequest"
            case chatUpdateReportStatusRequest = "chat.UpdateReportStatusRequest"
            case feedCreateCommentRequest = "feed.CreateCommentRequest"
            case feedCreatePostRequest = "feed.CreatePostRequest"
            case feedFeedResponse = "feed.FeedResponse"
            case guidesGuideCategoriesResponse = "guides.GuideCategoriesResponse"
            case guidesGuideDetail = "guides.GuideDetail"
            case guidesGuideSearchResponse = "guides.GuideSearchResponse"
            case guidesGuidesResponse = "guides.GuidesResponse"
            case packsAddPackItemBody = "packs.AddPackItemBody"
            case packsAnalyzeImageRequest = "packs.AnalyzeImageRequest"
            case packsCreatePackBody = "packs.CreatePackBody"
            case packsCreatePackWeightHistoryBody = "packs.CreatePackWeightHistoryBody"
            case packsErrorResponse = "packs.ErrorResponse"
            case packsGapAnalysisRequest = "packs.GapAnalysisRequest"
            case packsPackItem = "packs.PackItem"
            case packsPackWithWeights = "packs.PackWithWeights"
            case packsUpdatePackItemRequest = "packs.UpdatePackItemRequest"
            case packsUpdatePackRequest = "packs.UpdatePackRequest"
            case packTemplatesAIPackAnalysis = "packTemplates.AIPackAnalysis"
            case packTemplatesCreatePackTemplateItemRequest = "packTemplates.CreatePackTemplateItemRequest"
            case packTemplatesCreatePackTemplateRequest = "packTemplates.CreatePackTemplateRequest"
            case packTemplatesGenerateFromOnlineContentRequest = "packTemplates.GenerateFromOnlineContentRequest"
            case packTemplatesUpdatePackTemplateItemRequest = "packTemplates.UpdatePackTemplateItemRequest"
            case packTemplatesUpdatePackTemplateRequest = "packTemplates.UpdatePackTemplateRequest"
            case passwordResetForgotPasswordRequest = "passwordReset.ForgotPasswordRequest"
            case passwordResetResetPasswordRequest = "passwordReset.ResetPasswordRequest"
            case seasonSuggestionsSeasonSuggestionsRequest = "seasonSuggestions.SeasonSuggestionsRequest"
            case trailConditionsCreateTrailConditionReportRequest = "trailConditions.CreateTrailConditionReportRequest"
            case trailConditionsUpdateTrailConditionReportRequest = "trailConditions.UpdateTrailConditionReportRequest"
            case trailsRouteDetailRow = "trails.RouteDetailRow"
            case trailsRouteSearchRow = "trails.RouteSearchRow"
            case tripsCreateTripBody = "trips.CreateTripBody"
            case tripsTrip = "trips.Trip"
            case tripsUpdateTripBody = "trips.UpdateTripBody"
            case uploadPresignedUploadResponse = "upload.PresignedUploadResponse"
            case userErrorResponse = "user.ErrorResponse"
            case userUpdateUserRequest = "user.UpdateUserRequest"
            case userUpdateUserResponse = "user.UpdateUserResponse"
            case userUserProfile = "user.UserProfile"
            case weatherForecastResponse = "weather.ForecastResponse"
            case wildlifeWildlifeIdentifyRequest = "wildlife.WildlifeIdentifyRequest"
        }

        public init(catalogCatalogCategoriesResponse: [String]?, catalogCatalogCompareRequest: CatalogCatalogCompareRequest?, catalogCatalogETL: CatalogCatalogETL?, catalogCatalogItem: CatalogCatalogItem?, catalogCatalogItemsResponse: CatalogCatalogItemsResponse?, catalogCreateCatalogItemRequest: CatalogCreateCatalogItemRequest?, catalogErrorResponse: CatalogErrorResponse?, catalogUpdateCatalogItemRequest: CatalogUpdateCatalogItemRequest?, chatChatRequest: JSONAny?, chatCreateReportRequest: ChatCreateReportRequest?, chatUpdateReportStatusRequest: ChatUpdateReportStatusRequest?, feedCreateCommentRequest: FeedCreateCommentRequest?, feedCreatePostRequest: FeedCreatePostRequest?, feedFeedResponse: FeedFeedResponse?, guidesGuideCategoriesResponse: GuidesGuideCategoriesResponse?, guidesGuideDetail: GuidesGuideDetail?, guidesGuideSearchResponse: GuidesGuideSearchResponse?, guidesGuidesResponse: GuidesGuidesResponse?, packsAddPackItemBody: PacksAddPackItemBody?, packsAnalyzeImageRequest: PacksAnalyzeImageRequest?, packsCreatePackBody: PacksCreatePackBody?, packsCreatePackWeightHistoryBody: PacksCreatePackWeightHistoryBody?, packsErrorResponse: PacksErrorResponse?, packsGapAnalysisRequest: PacksGapAnalysisRequest?, packsPackItem: PacksPackItem?, packsPackWithWeights: PacksPackWithWeights?, packsUpdatePackItemRequest: PacksUpdatePackItemRequest?, packsUpdatePackRequest: PacksUpdatePackRequest?, packTemplatesAIPackAnalysis: PackTemplatesAIPackAnalysis?, packTemplatesCreatePackTemplateItemRequest: PackTemplatesCreatePackTemplateItemRequest?, packTemplatesCreatePackTemplateRequest: PackTemplatesCreatePackTemplateRequest?, packTemplatesGenerateFromOnlineContentRequest: PackTemplatesGenerateFromOnlineContentRequest?, packTemplatesUpdatePackTemplateItemRequest: PackTemplatesUpdatePackTemplateItemRequest?, packTemplatesUpdatePackTemplateRequest: PackTemplatesUpdatePackTemplateRequest?, passwordResetForgotPasswordRequest: PasswordResetForgotPasswordRequest?, passwordResetResetPasswordRequest: PasswordResetResetPasswordRequest?, seasonSuggestionsSeasonSuggestionsRequest: SeasonSuggestionsSeasonSuggestionsRequest?, trailConditionsCreateTrailConditionReportRequest: TrailConditionsCreateTrailConditionReportRequest?, trailConditionsUpdateTrailConditionReportRequest: TrailConditionsUpdateTrailConditionReportRequest?, trailsRouteDetailRow: TrailsRouteDetailRow?, trailsRouteSearchRow: TrailsRouteSearchRow?, tripsCreateTripBody: TripsCreateTripBody?, tripsTrip: TripsTrip?, tripsUpdateTripBody: TripsUpdateTripBody?, uploadPresignedUploadResponse: UploadPresignedUploadResponse?, userErrorResponse: UserErrorResponse?, userUpdateUserRequest: UserUpdateUserRequest?, userUpdateUserResponse: UserUpdateUserResponse?, userUserProfile: UserUserProfile?, weatherForecastResponse: WeatherForecastResponse?, wildlifeWildlifeIdentifyRequest: WildlifeWildlifeIdentifyRequest?) {
            self.catalogCatalogCategoriesResponse = catalogCatalogCategoriesResponse
            self.catalogCatalogCompareRequest = catalogCatalogCompareRequest
            self.catalogCatalogETL = catalogCatalogETL
            self.catalogCatalogItem = catalogCatalogItem
            self.catalogCatalogItemsResponse = catalogCatalogItemsResponse
            self.catalogCreateCatalogItemRequest = catalogCreateCatalogItemRequest
            self.catalogErrorResponse = catalogErrorResponse
            self.catalogUpdateCatalogItemRequest = catalogUpdateCatalogItemRequest
            self.chatChatRequest = chatChatRequest
            self.chatCreateReportRequest = chatCreateReportRequest
            self.chatUpdateReportStatusRequest = chatUpdateReportStatusRequest
            self.feedCreateCommentRequest = feedCreateCommentRequest
            self.feedCreatePostRequest = feedCreatePostRequest
            self.feedFeedResponse = feedFeedResponse
            self.guidesGuideCategoriesResponse = guidesGuideCategoriesResponse
            self.guidesGuideDetail = guidesGuideDetail
            self.guidesGuideSearchResponse = guidesGuideSearchResponse
            self.guidesGuidesResponse = guidesGuidesResponse
            self.packsAddPackItemBody = packsAddPackItemBody
            self.packsAnalyzeImageRequest = packsAnalyzeImageRequest
            self.packsCreatePackBody = packsCreatePackBody
            self.packsCreatePackWeightHistoryBody = packsCreatePackWeightHistoryBody
            self.packsErrorResponse = packsErrorResponse
            self.packsGapAnalysisRequest = packsGapAnalysisRequest
            self.packsPackItem = packsPackItem
            self.packsPackWithWeights = packsPackWithWeights
            self.packsUpdatePackItemRequest = packsUpdatePackItemRequest
            self.packsUpdatePackRequest = packsUpdatePackRequest
            self.packTemplatesAIPackAnalysis = packTemplatesAIPackAnalysis
            self.packTemplatesCreatePackTemplateItemRequest = packTemplatesCreatePackTemplateItemRequest
            self.packTemplatesCreatePackTemplateRequest = packTemplatesCreatePackTemplateRequest
            self.packTemplatesGenerateFromOnlineContentRequest = packTemplatesGenerateFromOnlineContentRequest
            self.packTemplatesUpdatePackTemplateItemRequest = packTemplatesUpdatePackTemplateItemRequest
            self.packTemplatesUpdatePackTemplateRequest = packTemplatesUpdatePackTemplateRequest
            self.passwordResetForgotPasswordRequest = passwordResetForgotPasswordRequest
            self.passwordResetResetPasswordRequest = passwordResetResetPasswordRequest
            self.seasonSuggestionsSeasonSuggestionsRequest = seasonSuggestionsSeasonSuggestionsRequest
            self.trailConditionsCreateTrailConditionReportRequest = trailConditionsCreateTrailConditionReportRequest
            self.trailConditionsUpdateTrailConditionReportRequest = trailConditionsUpdateTrailConditionReportRequest
            self.trailsRouteDetailRow = trailsRouteDetailRow
            self.trailsRouteSearchRow = trailsRouteSearchRow
            self.tripsCreateTripBody = tripsCreateTripBody
            self.tripsTrip = tripsTrip
            self.tripsUpdateTripBody = tripsUpdateTripBody
            self.uploadPresignedUploadResponse = uploadPresignedUploadResponse
            self.userErrorResponse = userErrorResponse
            self.userUpdateUserRequest = userUpdateUserRequest
            self.userUpdateUserResponse = userUpdateUserResponse
            self.userUserProfile = userUserProfile
            self.weatherForecastResponse = weatherForecastResponse
            self.wildlifeWildlifeIdentifyRequest = wildlifeWildlifeIdentifyRequest
        }
    }

    // MARK: PackRatComponents convenience initializers and mutators

    public extension PackRatComponents {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackRatComponents.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            catalogCatalogCategoriesResponse: [String]?? = nil,
            catalogCatalogCompareRequest: CatalogCatalogCompareRequest?? = nil,
            catalogCatalogETL: CatalogCatalogETL?? = nil,
            catalogCatalogItem: CatalogCatalogItem?? = nil,
            catalogCatalogItemsResponse: CatalogCatalogItemsResponse?? = nil,
            catalogCreateCatalogItemRequest: CatalogCreateCatalogItemRequest?? = nil,
            catalogErrorResponse: CatalogErrorResponse?? = nil,
            catalogUpdateCatalogItemRequest: CatalogUpdateCatalogItemRequest?? = nil,
            chatChatRequest: JSONAny?? = nil,
            chatCreateReportRequest: ChatCreateReportRequest?? = nil,
            chatUpdateReportStatusRequest: ChatUpdateReportStatusRequest?? = nil,
            feedCreateCommentRequest: FeedCreateCommentRequest?? = nil,
            feedCreatePostRequest: FeedCreatePostRequest?? = nil,
            feedFeedResponse: FeedFeedResponse?? = nil,
            guidesGuideCategoriesResponse: GuidesGuideCategoriesResponse?? = nil,
            guidesGuideDetail: GuidesGuideDetail?? = nil,
            guidesGuideSearchResponse: GuidesGuideSearchResponse?? = nil,
            guidesGuidesResponse: GuidesGuidesResponse?? = nil,
            packsAddPackItemBody: PacksAddPackItemBody?? = nil,
            packsAnalyzeImageRequest: PacksAnalyzeImageRequest?? = nil,
            packsCreatePackBody: PacksCreatePackBody?? = nil,
            packsCreatePackWeightHistoryBody: PacksCreatePackWeightHistoryBody?? = nil,
            packsErrorResponse: PacksErrorResponse?? = nil,
            packsGapAnalysisRequest: PacksGapAnalysisRequest?? = nil,
            packsPackItem: PacksPackItem?? = nil,
            packsPackWithWeights: PacksPackWithWeights?? = nil,
            packsUpdatePackItemRequest: PacksUpdatePackItemRequest?? = nil,
            packsUpdatePackRequest: PacksUpdatePackRequest?? = nil,
            packTemplatesAIPackAnalysis: PackTemplatesAIPackAnalysis?? = nil,
            packTemplatesCreatePackTemplateItemRequest: PackTemplatesCreatePackTemplateItemRequest?? = nil,
            packTemplatesCreatePackTemplateRequest: PackTemplatesCreatePackTemplateRequest?? = nil,
            packTemplatesGenerateFromOnlineContentRequest: PackTemplatesGenerateFromOnlineContentRequest?? = nil,
            packTemplatesUpdatePackTemplateItemRequest: PackTemplatesUpdatePackTemplateItemRequest?? = nil,
            packTemplatesUpdatePackTemplateRequest: PackTemplatesUpdatePackTemplateRequest?? = nil,
            passwordResetForgotPasswordRequest: PasswordResetForgotPasswordRequest?? = nil,
            passwordResetResetPasswordRequest: PasswordResetResetPasswordRequest?? = nil,
            seasonSuggestionsSeasonSuggestionsRequest: SeasonSuggestionsSeasonSuggestionsRequest?? = nil,
            trailConditionsCreateTrailConditionReportRequest: TrailConditionsCreateTrailConditionReportRequest?? = nil,
            trailConditionsUpdateTrailConditionReportRequest: TrailConditionsUpdateTrailConditionReportRequest?? = nil,
            trailsRouteDetailRow: TrailsRouteDetailRow?? = nil,
            trailsRouteSearchRow: TrailsRouteSearchRow?? = nil,
            tripsCreateTripBody: TripsCreateTripBody?? = nil,
            tripsTrip: TripsTrip?? = nil,
            tripsUpdateTripBody: TripsUpdateTripBody?? = nil,
            uploadPresignedUploadResponse: UploadPresignedUploadResponse?? = nil,
            userErrorResponse: UserErrorResponse?? = nil,
            userUpdateUserRequest: UserUpdateUserRequest?? = nil,
            userUpdateUserResponse: UserUpdateUserResponse?? = nil,
            userUserProfile: UserUserProfile?? = nil,
            weatherForecastResponse: WeatherForecastResponse?? = nil,
            wildlifeWildlifeIdentifyRequest: WildlifeWildlifeIdentifyRequest?? = nil
        ) -> PackRatComponents {
            return PackRatComponents(
                catalogCatalogCategoriesResponse: catalogCatalogCategoriesResponse ?? self.catalogCatalogCategoriesResponse,
                catalogCatalogCompareRequest: catalogCatalogCompareRequest ?? self.catalogCatalogCompareRequest,
                catalogCatalogETL: catalogCatalogETL ?? self.catalogCatalogETL,
                catalogCatalogItem: catalogCatalogItem ?? self.catalogCatalogItem,
                catalogCatalogItemsResponse: catalogCatalogItemsResponse ?? self.catalogCatalogItemsResponse,
                catalogCreateCatalogItemRequest: catalogCreateCatalogItemRequest ?? self.catalogCreateCatalogItemRequest,
                catalogErrorResponse: catalogErrorResponse ?? self.catalogErrorResponse,
                catalogUpdateCatalogItemRequest: catalogUpdateCatalogItemRequest ?? self.catalogUpdateCatalogItemRequest,
                chatChatRequest: chatChatRequest ?? self.chatChatRequest,
                chatCreateReportRequest: chatCreateReportRequest ?? self.chatCreateReportRequest,
                chatUpdateReportStatusRequest: chatUpdateReportStatusRequest ?? self.chatUpdateReportStatusRequest,
                feedCreateCommentRequest: feedCreateCommentRequest ?? self.feedCreateCommentRequest,
                feedCreatePostRequest: feedCreatePostRequest ?? self.feedCreatePostRequest,
                feedFeedResponse: feedFeedResponse ?? self.feedFeedResponse,
                guidesGuideCategoriesResponse: guidesGuideCategoriesResponse ?? self.guidesGuideCategoriesResponse,
                guidesGuideDetail: guidesGuideDetail ?? self.guidesGuideDetail,
                guidesGuideSearchResponse: guidesGuideSearchResponse ?? self.guidesGuideSearchResponse,
                guidesGuidesResponse: guidesGuidesResponse ?? self.guidesGuidesResponse,
                packsAddPackItemBody: packsAddPackItemBody ?? self.packsAddPackItemBody,
                packsAnalyzeImageRequest: packsAnalyzeImageRequest ?? self.packsAnalyzeImageRequest,
                packsCreatePackBody: packsCreatePackBody ?? self.packsCreatePackBody,
                packsCreatePackWeightHistoryBody: packsCreatePackWeightHistoryBody ?? self.packsCreatePackWeightHistoryBody,
                packsErrorResponse: packsErrorResponse ?? self.packsErrorResponse,
                packsGapAnalysisRequest: packsGapAnalysisRequest ?? self.packsGapAnalysisRequest,
                packsPackItem: packsPackItem ?? self.packsPackItem,
                packsPackWithWeights: packsPackWithWeights ?? self.packsPackWithWeights,
                packsUpdatePackItemRequest: packsUpdatePackItemRequest ?? self.packsUpdatePackItemRequest,
                packsUpdatePackRequest: packsUpdatePackRequest ?? self.packsUpdatePackRequest,
                packTemplatesAIPackAnalysis: packTemplatesAIPackAnalysis ?? self.packTemplatesAIPackAnalysis,
                packTemplatesCreatePackTemplateItemRequest: packTemplatesCreatePackTemplateItemRequest ?? self.packTemplatesCreatePackTemplateItemRequest,
                packTemplatesCreatePackTemplateRequest: packTemplatesCreatePackTemplateRequest ?? self.packTemplatesCreatePackTemplateRequest,
                packTemplatesGenerateFromOnlineContentRequest: packTemplatesGenerateFromOnlineContentRequest ?? self.packTemplatesGenerateFromOnlineContentRequest,
                packTemplatesUpdatePackTemplateItemRequest: packTemplatesUpdatePackTemplateItemRequest ?? self.packTemplatesUpdatePackTemplateItemRequest,
                packTemplatesUpdatePackTemplateRequest: packTemplatesUpdatePackTemplateRequest ?? self.packTemplatesUpdatePackTemplateRequest,
                passwordResetForgotPasswordRequest: passwordResetForgotPasswordRequest ?? self.passwordResetForgotPasswordRequest,
                passwordResetResetPasswordRequest: passwordResetResetPasswordRequest ?? self.passwordResetResetPasswordRequest,
                seasonSuggestionsSeasonSuggestionsRequest: seasonSuggestionsSeasonSuggestionsRequest ?? self.seasonSuggestionsSeasonSuggestionsRequest,
                trailConditionsCreateTrailConditionReportRequest: trailConditionsCreateTrailConditionReportRequest ?? self.trailConditionsCreateTrailConditionReportRequest,
                trailConditionsUpdateTrailConditionReportRequest: trailConditionsUpdateTrailConditionReportRequest ?? self.trailConditionsUpdateTrailConditionReportRequest,
                trailsRouteDetailRow: trailsRouteDetailRow ?? self.trailsRouteDetailRow,
                trailsRouteSearchRow: trailsRouteSearchRow ?? self.trailsRouteSearchRow,
                tripsCreateTripBody: tripsCreateTripBody ?? self.tripsCreateTripBody,
                tripsTrip: tripsTrip ?? self.tripsTrip,
                tripsUpdateTripBody: tripsUpdateTripBody ?? self.tripsUpdateTripBody,
                uploadPresignedUploadResponse: uploadPresignedUploadResponse ?? self.uploadPresignedUploadResponse,
                userErrorResponse: userErrorResponse ?? self.userErrorResponse,
                userUpdateUserRequest: userUpdateUserRequest ?? self.userUpdateUserRequest,
                userUpdateUserResponse: userUpdateUserResponse ?? self.userUpdateUserResponse,
                userUserProfile: userUserProfile ?? self.userUserProfile,
                weatherForecastResponse: weatherForecastResponse ?? self.weatherForecastResponse,
                wildlifeWildlifeIdentifyRequest: wildlifeWildlifeIdentifyRequest ?? self.wildlifeWildlifeIdentifyRequest
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogCompareRequest
    public struct CatalogCatalogCompareRequest: Codable, Equatable {
        public let ids: [Int]

        public init(ids: [Int]) {
            self.ids = ids
        }
    }

    // MARK: CatalogCatalogCompareRequest convenience initializers and mutators

    public extension CatalogCatalogCompareRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogCompareRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            ids: [Int]? = nil
        ) -> CatalogCatalogCompareRequest {
            return CatalogCatalogCompareRequest(
                ids: ids ?? self.ids
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogETL
    public struct CatalogCatalogETL: Codable, Equatable {
        public let chunks: [String]
        public let filename, scraperRevision, source: String

        public init(chunks: [String], filename: String, scraperRevision: String, source: String) {
            self.chunks = chunks
            self.filename = filename
            self.scraperRevision = scraperRevision
            self.source = source
        }
    }

    // MARK: CatalogCatalogETL convenience initializers and mutators

    public extension CatalogCatalogETL {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogETL.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            chunks: [String]? = nil,
            filename: String? = nil,
            scraperRevision: String? = nil,
            source: String? = nil
        ) -> CatalogCatalogETL {
            return CatalogCatalogETL(
                chunks: chunks ?? self.chunks,
                filename: filename ?? self.filename,
                scraperRevision: scraperRevision ?? self.scraperRevision,
                source: source ?? self.source
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItem
    public struct CatalogCatalogItem: Codable, Equatable {
        public let availability: Availability
        public let brand: String
        public let categories: [String]
        public let color, condition: String
        public let createdAt: Date
        public let currency, description: String
        public let faqs: [CatalogCatalogItemFAQ]?
        public let id: Int
        public let images: [String]
        public let links: [CatalogCatalogItemLink]?
        public let material, model, name: String
        public let price: Double
        public let productSku, productURL: String
        public let qas: [CatalogCatalogItemQA]?
        public let ratingValue: Double
        public let reviewCount: Int
        public let reviews: [CatalogCatalogItemReview]?
        public let seller, size, sku: String
        public let techs: [String: String]?
        public let updatedAt: Date
        public let usageCount: Int?
        public let variants: [CatalogCatalogItemVariant]?
        public let weight: Double
        public let weightUnit: WeightUnit

        public enum CodingKeys: String, CodingKey {
            case availability, brand, categories, color, condition, createdAt, currency, description, faqs, id, images, links, material, model, name, price, productSku
            case productURL = "productUrl"
            case qas, ratingValue, reviewCount, reviews, seller, size, sku, techs, updatedAt, usageCount, variants, weight, weightUnit
        }

        public init(availability: Availability, brand: String, categories: [String], color: String, condition: String, createdAt: Date, currency: String, description: String, faqs: [CatalogCatalogItemFAQ]?, id: Int, images: [String], links: [CatalogCatalogItemLink]?, material: String, model: String, name: String, price: Double, productSku: String, productURL: String, qas: [CatalogCatalogItemQA]?, ratingValue: Double, reviewCount: Int, reviews: [CatalogCatalogItemReview]?, seller: String, size: String, sku: String, techs: [String: String]?, updatedAt: Date, usageCount: Int?, variants: [CatalogCatalogItemVariant]?, weight: Double, weightUnit: WeightUnit) {
            self.availability = availability
            self.brand = brand
            self.categories = categories
            self.color = color
            self.condition = condition
            self.createdAt = createdAt
            self.currency = currency
            self.description = description
            self.faqs = faqs
            self.id = id
            self.images = images
            self.links = links
            self.material = material
            self.model = model
            self.name = name
            self.price = price
            self.productSku = productSku
            self.productURL = productURL
            self.qas = qas
            self.ratingValue = ratingValue
            self.reviewCount = reviewCount
            self.reviews = reviews
            self.seller = seller
            self.size = size
            self.sku = sku
            self.techs = techs
            self.updatedAt = updatedAt
            self.usageCount = usageCount
            self.variants = variants
            self.weight = weight
            self.weightUnit = weightUnit
        }
    }

    // MARK: CatalogCatalogItem convenience initializers and mutators

    public extension CatalogCatalogItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            availability: Availability? = nil,
            brand: String? = nil,
            categories: [String]? = nil,
            color: String? = nil,
            condition: String? = nil,
            createdAt: Date? = nil,
            currency: String? = nil,
            description: String? = nil,
            faqs: [CatalogCatalogItemFAQ]?? = nil,
            id: Int? = nil,
            images: [String]? = nil,
            links: [CatalogCatalogItemLink]?? = nil,
            material: String? = nil,
            model: String? = nil,
            name: String? = nil,
            price: Double? = nil,
            productSku: String? = nil,
            productURL: String? = nil,
            qas: [CatalogCatalogItemQA]?? = nil,
            ratingValue: Double? = nil,
            reviewCount: Int? = nil,
            reviews: [CatalogCatalogItemReview]?? = nil,
            seller: String? = nil,
            size: String? = nil,
            sku: String? = nil,
            techs: [String: String]?? = nil,
            updatedAt: Date? = nil,
            usageCount: Int?? = nil,
            variants: [CatalogCatalogItemVariant]?? = nil,
            weight: Double? = nil,
            weightUnit: WeightUnit? = nil
        ) -> CatalogCatalogItem {
            return CatalogCatalogItem(
                availability: availability ?? self.availability,
                brand: brand ?? self.brand,
                categories: categories ?? self.categories,
                color: color ?? self.color,
                condition: condition ?? self.condition,
                createdAt: createdAt ?? self.createdAt,
                currency: currency ?? self.currency,
                description: description ?? self.description,
                faqs: faqs ?? self.faqs,
                id: id ?? self.id,
                images: images ?? self.images,
                links: links ?? self.links,
                material: material ?? self.material,
                model: model ?? self.model,
                name: name ?? self.name,
                price: price ?? self.price,
                productSku: productSku ?? self.productSku,
                productURL: productURL ?? self.productURL,
                qas: qas ?? self.qas,
                ratingValue: ratingValue ?? self.ratingValue,
                reviewCount: reviewCount ?? self.reviewCount,
                reviews: reviews ?? self.reviews,
                seller: seller ?? self.seller,
                size: size ?? self.size,
                sku: sku ?? self.sku,
                techs: techs ?? self.techs,
                updatedAt: updatedAt ?? self.updatedAt,
                usageCount: usageCount ?? self.usageCount,
                variants: variants ?? self.variants,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    public enum Availability: String, Codable, Equatable {
        case inStock = "in_stock"
        case outOfStock = "out_of_stock"
        case preorder = "preorder"
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItemFAQ
    public struct CatalogCatalogItemFAQ: Codable, Equatable {
        public let answer, question: String

        public init(answer: String, question: String) {
            self.answer = answer
            self.question = question
        }
    }

    // MARK: CatalogCatalogItemFAQ convenience initializers and mutators

    public extension CatalogCatalogItemFAQ {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItemFAQ.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answer: String? = nil,
            question: String? = nil
        ) -> CatalogCatalogItemFAQ {
            return CatalogCatalogItemFAQ(
                answer: answer ?? self.answer,
                question: question ?? self.question
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItemLink
    public struct CatalogCatalogItemLink: Codable, Equatable {
        public let title, url: String

        public init(title: String, url: String) {
            self.title = title
            self.url = url
        }
    }

    // MARK: CatalogCatalogItemLink convenience initializers and mutators

    public extension CatalogCatalogItemLink {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItemLink.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            title: String? = nil,
            url: String? = nil
        ) -> CatalogCatalogItemLink {
            return CatalogCatalogItemLink(
                title: title ?? self.title,
                url: url ?? self.url
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItemQA
    public struct CatalogCatalogItemQA: Codable, Equatable {
        public let answers: [PurpleAnswer]
        public let date, question: String
        public let user: String?

        public init(answers: [PurpleAnswer], date: String, question: String, user: String?) {
            self.answers = answers
            self.date = date
            self.question = question
            self.user = user
        }
    }

    // MARK: CatalogCatalogItemQA convenience initializers and mutators

    public extension CatalogCatalogItemQA {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItemQA.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answers: [PurpleAnswer]? = nil,
            date: String? = nil,
            question: String? = nil,
            user: String?? = nil
        ) -> CatalogCatalogItemQA {
            return CatalogCatalogItemQA(
                answers: answers ?? self.answers,
                date: date ?? self.date,
                question: question ?? self.question,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PurpleAnswer
    public struct PurpleAnswer: Codable, Equatable {
        public let a, date: String
        public let upvotes: Double?
        public let user: String?

        public init(a: String, date: String, upvotes: Double?, user: String?) {
            self.a = a
            self.date = date
            self.upvotes = upvotes
            self.user = user
        }
    }

    // MARK: PurpleAnswer convenience initializers and mutators

    public extension PurpleAnswer {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PurpleAnswer.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            a: String? = nil,
            date: String? = nil,
            upvotes: Double?? = nil,
            user: String?? = nil
        ) -> PurpleAnswer {
            return PurpleAnswer(
                a: a ?? self.a,
                date: date ?? self.date,
                upvotes: upvotes ?? self.upvotes,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItemReview
    public struct CatalogCatalogItemReview: Codable, Equatable {
        public let context: [String: String]?
        public let date: String?
        public let downvotes: Double?
        public let images: [String]?
        public let rating: Double
        public let recommends: Bool?
        public let text, title: String?
        public let upvotes: Double?
        public let userAvatar, userName: String?
        public let verified: Bool?

        public enum CodingKeys: String, CodingKey {
            case context, date, downvotes, images, rating, recommends, text, title, upvotes
            case userAvatar = "user_avatar"
            case userName = "user_name"
            case verified
        }

        public init(context: [String: String]?, date: String?, downvotes: Double?, images: [String]?, rating: Double, recommends: Bool?, text: String?, title: String?, upvotes: Double?, userAvatar: String?, userName: String?, verified: Bool?) {
            self.context = context
            self.date = date
            self.downvotes = downvotes
            self.images = images
            self.rating = rating
            self.recommends = recommends
            self.text = text
            self.title = title
            self.upvotes = upvotes
            self.userAvatar = userAvatar
            self.userName = userName
            self.verified = verified
        }
    }

    // MARK: CatalogCatalogItemReview convenience initializers and mutators

    public extension CatalogCatalogItemReview {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItemReview.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            context: [String: String]?? = nil,
            date: String?? = nil,
            downvotes: Double?? = nil,
            images: [String]?? = nil,
            rating: Double? = nil,
            recommends: Bool?? = nil,
            text: String?? = nil,
            title: String?? = nil,
            upvotes: Double?? = nil,
            userAvatar: String?? = nil,
            userName: String?? = nil,
            verified: Bool?? = nil
        ) -> CatalogCatalogItemReview {
            return CatalogCatalogItemReview(
                context: context ?? self.context,
                date: date ?? self.date,
                downvotes: downvotes ?? self.downvotes,
                images: images ?? self.images,
                rating: rating ?? self.rating,
                recommends: recommends ?? self.recommends,
                text: text ?? self.text,
                title: title ?? self.title,
                upvotes: upvotes ?? self.upvotes,
                userAvatar: userAvatar ?? self.userAvatar,
                userName: userName ?? self.userName,
                verified: verified ?? self.verified
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItemVariant
    public struct CatalogCatalogItemVariant: Codable, Equatable {
        public let attribute: String
        public let values: [String]

        public init(attribute: String, values: [String]) {
            self.attribute = attribute
            self.values = values
        }
    }

    // MARK: CatalogCatalogItemVariant convenience initializers and mutators

    public extension CatalogCatalogItemVariant {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItemVariant.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            attribute: String? = nil,
            values: [String]? = nil
        ) -> CatalogCatalogItemVariant {
            return CatalogCatalogItemVariant(
                attribute: attribute ?? self.attribute,
                values: values ?? self.values
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    public enum WeightUnit: String, Codable, Equatable {
        case g = "g"
        case kg = "kg"
        case lb = "lb"
        case oz = "oz"
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItemsResponse
    public struct CatalogCatalogItemsResponse: Codable, Equatable {
        public let items: [CatalogCatalogItemsResponseItem]
        public let limit, page, totalCount, totalPages: Double

        public init(items: [CatalogCatalogItemsResponseItem], limit: Double, page: Double, totalCount: Double, totalPages: Double) {
            self.items = items
            self.limit = limit
            self.page = page
            self.totalCount = totalCount
            self.totalPages = totalPages
        }
    }

    // MARK: CatalogCatalogItemsResponse convenience initializers and mutators

    public extension CatalogCatalogItemsResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItemsResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            items: [CatalogCatalogItemsResponseItem]? = nil,
            limit: Double? = nil,
            page: Double? = nil,
            totalCount: Double? = nil,
            totalPages: Double? = nil
        ) -> CatalogCatalogItemsResponse {
            return CatalogCatalogItemsResponse(
                items: items ?? self.items,
                limit: limit ?? self.limit,
                page: page ?? self.page,
                totalCount: totalCount ?? self.totalCount,
                totalPages: totalPages ?? self.totalPages
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCatalogItemsResponseItem
    public struct CatalogCatalogItemsResponseItem: Codable, Equatable {
        public let availability: Availability
        public let brand: String
        public let categories: [String]
        public let color, condition: String
        public let createdAt: Date
        public let currency, description: String
        public let faqs: [ItemFAQ]?
        public let id: Int
        public let images: [String]
        public let links: [ItemLink]?
        public let material, model, name: String
        public let price: Double
        public let productSku, productURL: String
        public let qas: [ItemQA]?
        public let ratingValue: Double
        public let reviewCount: Int
        public let reviews: [ItemReview]?
        public let seller, size, sku: String
        public let techs: [String: String]?
        public let updatedAt: Date
        public let usageCount: Int?
        public let variants: [ItemVariant]?
        public let weight: Double
        public let weightUnit: WeightUnit

        public enum CodingKeys: String, CodingKey {
            case availability, brand, categories, color, condition, createdAt, currency, description, faqs, id, images, links, material, model, name, price, productSku
            case productURL = "productUrl"
            case qas, ratingValue, reviewCount, reviews, seller, size, sku, techs, updatedAt, usageCount, variants, weight, weightUnit
        }

        public init(availability: Availability, brand: String, categories: [String], color: String, condition: String, createdAt: Date, currency: String, description: String, faqs: [ItemFAQ]?, id: Int, images: [String], links: [ItemLink]?, material: String, model: String, name: String, price: Double, productSku: String, productURL: String, qas: [ItemQA]?, ratingValue: Double, reviewCount: Int, reviews: [ItemReview]?, seller: String, size: String, sku: String, techs: [String: String]?, updatedAt: Date, usageCount: Int?, variants: [ItemVariant]?, weight: Double, weightUnit: WeightUnit) {
            self.availability = availability
            self.brand = brand
            self.categories = categories
            self.color = color
            self.condition = condition
            self.createdAt = createdAt
            self.currency = currency
            self.description = description
            self.faqs = faqs
            self.id = id
            self.images = images
            self.links = links
            self.material = material
            self.model = model
            self.name = name
            self.price = price
            self.productSku = productSku
            self.productURL = productURL
            self.qas = qas
            self.ratingValue = ratingValue
            self.reviewCount = reviewCount
            self.reviews = reviews
            self.seller = seller
            self.size = size
            self.sku = sku
            self.techs = techs
            self.updatedAt = updatedAt
            self.usageCount = usageCount
            self.variants = variants
            self.weight = weight
            self.weightUnit = weightUnit
        }
    }

    // MARK: CatalogCatalogItemsResponseItem convenience initializers and mutators

    public extension CatalogCatalogItemsResponseItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCatalogItemsResponseItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            availability: Availability? = nil,
            brand: String? = nil,
            categories: [String]? = nil,
            color: String? = nil,
            condition: String? = nil,
            createdAt: Date? = nil,
            currency: String? = nil,
            description: String? = nil,
            faqs: [ItemFAQ]?? = nil,
            id: Int? = nil,
            images: [String]? = nil,
            links: [ItemLink]?? = nil,
            material: String? = nil,
            model: String? = nil,
            name: String? = nil,
            price: Double? = nil,
            productSku: String? = nil,
            productURL: String? = nil,
            qas: [ItemQA]?? = nil,
            ratingValue: Double? = nil,
            reviewCount: Int? = nil,
            reviews: [ItemReview]?? = nil,
            seller: String? = nil,
            size: String? = nil,
            sku: String? = nil,
            techs: [String: String]?? = nil,
            updatedAt: Date? = nil,
            usageCount: Int?? = nil,
            variants: [ItemVariant]?? = nil,
            weight: Double? = nil,
            weightUnit: WeightUnit? = nil
        ) -> CatalogCatalogItemsResponseItem {
            return CatalogCatalogItemsResponseItem(
                availability: availability ?? self.availability,
                brand: brand ?? self.brand,
                categories: categories ?? self.categories,
                color: color ?? self.color,
                condition: condition ?? self.condition,
                createdAt: createdAt ?? self.createdAt,
                currency: currency ?? self.currency,
                description: description ?? self.description,
                faqs: faqs ?? self.faqs,
                id: id ?? self.id,
                images: images ?? self.images,
                links: links ?? self.links,
                material: material ?? self.material,
                model: model ?? self.model,
                name: name ?? self.name,
                price: price ?? self.price,
                productSku: productSku ?? self.productSku,
                productURL: productURL ?? self.productURL,
                qas: qas ?? self.qas,
                ratingValue: ratingValue ?? self.ratingValue,
                reviewCount: reviewCount ?? self.reviewCount,
                reviews: reviews ?? self.reviews,
                seller: seller ?? self.seller,
                size: size ?? self.size,
                sku: sku ?? self.sku,
                techs: techs ?? self.techs,
                updatedAt: updatedAt ?? self.updatedAt,
                usageCount: usageCount ?? self.usageCount,
                variants: variants ?? self.variants,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - ItemFAQ
    public struct ItemFAQ: Codable, Equatable {
        public let answer, question: String

        public init(answer: String, question: String) {
            self.answer = answer
            self.question = question
        }
    }

    // MARK: ItemFAQ convenience initializers and mutators

    public extension ItemFAQ {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(ItemFAQ.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answer: String? = nil,
            question: String? = nil
        ) -> ItemFAQ {
            return ItemFAQ(
                answer: answer ?? self.answer,
                question: question ?? self.question
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - ItemLink
    public struct ItemLink: Codable, Equatable {
        public let title, url: String

        public init(title: String, url: String) {
            self.title = title
            self.url = url
        }
    }

    // MARK: ItemLink convenience initializers and mutators

    public extension ItemLink {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(ItemLink.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            title: String? = nil,
            url: String? = nil
        ) -> ItemLink {
            return ItemLink(
                title: title ?? self.title,
                url: url ?? self.url
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - ItemQA
    public struct ItemQA: Codable, Equatable {
        public let answers: [FluffyAnswer]
        public let date, question: String
        public let user: String?

        public init(answers: [FluffyAnswer], date: String, question: String, user: String?) {
            self.answers = answers
            self.date = date
            self.question = question
            self.user = user
        }
    }

    // MARK: ItemQA convenience initializers and mutators

    public extension ItemQA {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(ItemQA.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answers: [FluffyAnswer]? = nil,
            date: String? = nil,
            question: String? = nil,
            user: String?? = nil
        ) -> ItemQA {
            return ItemQA(
                answers: answers ?? self.answers,
                date: date ?? self.date,
                question: question ?? self.question,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - FluffyAnswer
    public struct FluffyAnswer: Codable, Equatable {
        public let a, date: String
        public let upvotes: Double?
        public let user: String?

        public init(a: String, date: String, upvotes: Double?, user: String?) {
            self.a = a
            self.date = date
            self.upvotes = upvotes
            self.user = user
        }
    }

    // MARK: FluffyAnswer convenience initializers and mutators

    public extension FluffyAnswer {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(FluffyAnswer.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            a: String? = nil,
            date: String? = nil,
            upvotes: Double?? = nil,
            user: String?? = nil
        ) -> FluffyAnswer {
            return FluffyAnswer(
                a: a ?? self.a,
                date: date ?? self.date,
                upvotes: upvotes ?? self.upvotes,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - ItemReview
    public struct ItemReview: Codable, Equatable {
        public let context: [String: String]?
        public let date: String?
        public let downvotes: Double?
        public let images: [String]?
        public let rating: Double
        public let recommends: Bool?
        public let text, title: String?
        public let upvotes: Double?
        public let userAvatar, userName: String?
        public let verified: Bool?

        public enum CodingKeys: String, CodingKey {
            case context, date, downvotes, images, rating, recommends, text, title, upvotes
            case userAvatar = "user_avatar"
            case userName = "user_name"
            case verified
        }

        public init(context: [String: String]?, date: String?, downvotes: Double?, images: [String]?, rating: Double, recommends: Bool?, text: String?, title: String?, upvotes: Double?, userAvatar: String?, userName: String?, verified: Bool?) {
            self.context = context
            self.date = date
            self.downvotes = downvotes
            self.images = images
            self.rating = rating
            self.recommends = recommends
            self.text = text
            self.title = title
            self.upvotes = upvotes
            self.userAvatar = userAvatar
            self.userName = userName
            self.verified = verified
        }
    }

    // MARK: ItemReview convenience initializers and mutators

    public extension ItemReview {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(ItemReview.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            context: [String: String]?? = nil,
            date: String?? = nil,
            downvotes: Double?? = nil,
            images: [String]?? = nil,
            rating: Double? = nil,
            recommends: Bool?? = nil,
            text: String?? = nil,
            title: String?? = nil,
            upvotes: Double?? = nil,
            userAvatar: String?? = nil,
            userName: String?? = nil,
            verified: Bool?? = nil
        ) -> ItemReview {
            return ItemReview(
                context: context ?? self.context,
                date: date ?? self.date,
                downvotes: downvotes ?? self.downvotes,
                images: images ?? self.images,
                rating: rating ?? self.rating,
                recommends: recommends ?? self.recommends,
                text: text ?? self.text,
                title: title ?? self.title,
                upvotes: upvotes ?? self.upvotes,
                userAvatar: userAvatar ?? self.userAvatar,
                userName: userName ?? self.userName,
                verified: verified ?? self.verified
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - ItemVariant
    public struct ItemVariant: Codable, Equatable {
        public let attribute: String
        public let values: [String]

        public init(attribute: String, values: [String]) {
            self.attribute = attribute
            self.values = values
        }
    }

    // MARK: ItemVariant convenience initializers and mutators

    public extension ItemVariant {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(ItemVariant.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            attribute: String? = nil,
            values: [String]? = nil
        ) -> ItemVariant {
            return ItemVariant(
                attribute: attribute ?? self.attribute,
                values: values ?? self.values
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCreateCatalogItemRequest
    public struct CatalogCreateCatalogItemRequest: Codable, Equatable {
        public let availability: Availability?
        public let brand: String?
        public let categories: [String]?
        public let color, condition, currency, description: String?
        public let faqs: [CatalogCreateCatalogItemRequestFAQ]?
        public let images: [String]?
        public let links: [CatalogCreateCatalogItemRequestLink]?
        public let material, model: String?
        public let name: String
        public let price: Double?
        public let productSku: String?
        public let productURL: String
        public let qas: [CatalogCreateCatalogItemRequestQA]?
        public let ratingValue: Double?
        public let reviewCount: Double?
        public let reviews: [CatalogCreateCatalogItemRequestReview]?
        public let seller, size: String?
        public let sku: String
        public let techs: [String: String]?
        public let variants: [CatalogCreateCatalogItemRequestVariant]?
        public let weight: Double
        public let weightUnit: WeightUnit

        public enum CodingKeys: String, CodingKey {
            case availability, brand, categories, color, condition, currency, description, faqs, images, links, material, model, name, price, productSku
            case productURL = "productUrl"
            case qas, ratingValue, reviewCount, reviews, seller, size, sku, techs, variants, weight, weightUnit
        }

        public init(availability: Availability?, brand: String?, categories: [String]?, color: String?, condition: String?, currency: String?, description: String?, faqs: [CatalogCreateCatalogItemRequestFAQ]?, images: [String]?, links: [CatalogCreateCatalogItemRequestLink]?, material: String?, model: String?, name: String, price: Double?, productSku: String?, productURL: String, qas: [CatalogCreateCatalogItemRequestQA]?, ratingValue: Double?, reviewCount: Double?, reviews: [CatalogCreateCatalogItemRequestReview]?, seller: String?, size: String?, sku: String, techs: [String: String]?, variants: [CatalogCreateCatalogItemRequestVariant]?, weight: Double, weightUnit: WeightUnit) {
            self.availability = availability
            self.brand = brand
            self.categories = categories
            self.color = color
            self.condition = condition
            self.currency = currency
            self.description = description
            self.faqs = faqs
            self.images = images
            self.links = links
            self.material = material
            self.model = model
            self.name = name
            self.price = price
            self.productSku = productSku
            self.productURL = productURL
            self.qas = qas
            self.ratingValue = ratingValue
            self.reviewCount = reviewCount
            self.reviews = reviews
            self.seller = seller
            self.size = size
            self.sku = sku
            self.techs = techs
            self.variants = variants
            self.weight = weight
            self.weightUnit = weightUnit
        }
    }

    // MARK: CatalogCreateCatalogItemRequest convenience initializers and mutators

    public extension CatalogCreateCatalogItemRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCreateCatalogItemRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            availability: Availability?? = nil,
            brand: String?? = nil,
            categories: [String]?? = nil,
            color: String?? = nil,
            condition: String?? = nil,
            currency: String?? = nil,
            description: String?? = nil,
            faqs: [CatalogCreateCatalogItemRequestFAQ]?? = nil,
            images: [String]?? = nil,
            links: [CatalogCreateCatalogItemRequestLink]?? = nil,
            material: String?? = nil,
            model: String?? = nil,
            name: String? = nil,
            price: Double?? = nil,
            productSku: String?? = nil,
            productURL: String? = nil,
            qas: [CatalogCreateCatalogItemRequestQA]?? = nil,
            ratingValue: Double?? = nil,
            reviewCount: Double?? = nil,
            reviews: [CatalogCreateCatalogItemRequestReview]?? = nil,
            seller: String?? = nil,
            size: String?? = nil,
            sku: String? = nil,
            techs: [String: String]?? = nil,
            variants: [CatalogCreateCatalogItemRequestVariant]?? = nil,
            weight: Double? = nil,
            weightUnit: WeightUnit? = nil
        ) -> CatalogCreateCatalogItemRequest {
            return CatalogCreateCatalogItemRequest(
                availability: availability ?? self.availability,
                brand: brand ?? self.brand,
                categories: categories ?? self.categories,
                color: color ?? self.color,
                condition: condition ?? self.condition,
                currency: currency ?? self.currency,
                description: description ?? self.description,
                faqs: faqs ?? self.faqs,
                images: images ?? self.images,
                links: links ?? self.links,
                material: material ?? self.material,
                model: model ?? self.model,
                name: name ?? self.name,
                price: price ?? self.price,
                productSku: productSku ?? self.productSku,
                productURL: productURL ?? self.productURL,
                qas: qas ?? self.qas,
                ratingValue: ratingValue ?? self.ratingValue,
                reviewCount: reviewCount ?? self.reviewCount,
                reviews: reviews ?? self.reviews,
                seller: seller ?? self.seller,
                size: size ?? self.size,
                sku: sku ?? self.sku,
                techs: techs ?? self.techs,
                variants: variants ?? self.variants,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCreateCatalogItemRequestFAQ
    public struct CatalogCreateCatalogItemRequestFAQ: Codable, Equatable {
        public let answer, question: String

        public init(answer: String, question: String) {
            self.answer = answer
            self.question = question
        }
    }

    // MARK: CatalogCreateCatalogItemRequestFAQ convenience initializers and mutators

    public extension CatalogCreateCatalogItemRequestFAQ {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCreateCatalogItemRequestFAQ.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answer: String? = nil,
            question: String? = nil
        ) -> CatalogCreateCatalogItemRequestFAQ {
            return CatalogCreateCatalogItemRequestFAQ(
                answer: answer ?? self.answer,
                question: question ?? self.question
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCreateCatalogItemRequestLink
    public struct CatalogCreateCatalogItemRequestLink: Codable, Equatable {
        public let title, url: String

        public init(title: String, url: String) {
            self.title = title
            self.url = url
        }
    }

    // MARK: CatalogCreateCatalogItemRequestLink convenience initializers and mutators

    public extension CatalogCreateCatalogItemRequestLink {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCreateCatalogItemRequestLink.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            title: String? = nil,
            url: String? = nil
        ) -> CatalogCreateCatalogItemRequestLink {
            return CatalogCreateCatalogItemRequestLink(
                title: title ?? self.title,
                url: url ?? self.url
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCreateCatalogItemRequestQA
    public struct CatalogCreateCatalogItemRequestQA: Codable, Equatable {
        public let answers: [TentacledAnswer]
        public let date, question: String
        public let user: String?

        public init(answers: [TentacledAnswer], date: String, question: String, user: String?) {
            self.answers = answers
            self.date = date
            self.question = question
            self.user = user
        }
    }

    // MARK: CatalogCreateCatalogItemRequestQA convenience initializers and mutators

    public extension CatalogCreateCatalogItemRequestQA {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCreateCatalogItemRequestQA.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answers: [TentacledAnswer]? = nil,
            date: String? = nil,
            question: String? = nil,
            user: String?? = nil
        ) -> CatalogCreateCatalogItemRequestQA {
            return CatalogCreateCatalogItemRequestQA(
                answers: answers ?? self.answers,
                date: date ?? self.date,
                question: question ?? self.question,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TentacledAnswer
    public struct TentacledAnswer: Codable, Equatable {
        public let a, date: String
        public let upvotes: Double?
        public let user: String?

        public init(a: String, date: String, upvotes: Double?, user: String?) {
            self.a = a
            self.date = date
            self.upvotes = upvotes
            self.user = user
        }
    }

    // MARK: TentacledAnswer convenience initializers and mutators

    public extension TentacledAnswer {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TentacledAnswer.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            a: String? = nil,
            date: String? = nil,
            upvotes: Double?? = nil,
            user: String?? = nil
        ) -> TentacledAnswer {
            return TentacledAnswer(
                a: a ?? self.a,
                date: date ?? self.date,
                upvotes: upvotes ?? self.upvotes,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCreateCatalogItemRequestReview
    public struct CatalogCreateCatalogItemRequestReview: Codable, Equatable {
        public let context: [String: String]?
        public let date: String
        public let downvotes: Double?
        public let images: [String]?
        public let rating: Double
        public let recommends: Bool?
        public let text, title: String
        public let upvotes: Double?
        public let userAvatar: String?
        public let userName: String
        public let verified: Bool?

        public enum CodingKeys: String, CodingKey {
            case context, date, downvotes, images, rating, recommends, text, title, upvotes
            case userAvatar = "user_avatar"
            case userName = "user_name"
            case verified
        }

        public init(context: [String: String]?, date: String, downvotes: Double?, images: [String]?, rating: Double, recommends: Bool?, text: String, title: String, upvotes: Double?, userAvatar: String?, userName: String, verified: Bool?) {
            self.context = context
            self.date = date
            self.downvotes = downvotes
            self.images = images
            self.rating = rating
            self.recommends = recommends
            self.text = text
            self.title = title
            self.upvotes = upvotes
            self.userAvatar = userAvatar
            self.userName = userName
            self.verified = verified
        }
    }

    // MARK: CatalogCreateCatalogItemRequestReview convenience initializers and mutators

    public extension CatalogCreateCatalogItemRequestReview {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCreateCatalogItemRequestReview.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            context: [String: String]?? = nil,
            date: String? = nil,
            downvotes: Double?? = nil,
            images: [String]?? = nil,
            rating: Double? = nil,
            recommends: Bool?? = nil,
            text: String? = nil,
            title: String? = nil,
            upvotes: Double?? = nil,
            userAvatar: String?? = nil,
            userName: String? = nil,
            verified: Bool?? = nil
        ) -> CatalogCreateCatalogItemRequestReview {
            return CatalogCreateCatalogItemRequestReview(
                context: context ?? self.context,
                date: date ?? self.date,
                downvotes: downvotes ?? self.downvotes,
                images: images ?? self.images,
                rating: rating ?? self.rating,
                recommends: recommends ?? self.recommends,
                text: text ?? self.text,
                title: title ?? self.title,
                upvotes: upvotes ?? self.upvotes,
                userAvatar: userAvatar ?? self.userAvatar,
                userName: userName ?? self.userName,
                verified: verified ?? self.verified
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogCreateCatalogItemRequestVariant
    public struct CatalogCreateCatalogItemRequestVariant: Codable, Equatable {
        public let attribute: String
        public let values: [String]

        public init(attribute: String, values: [String]) {
            self.attribute = attribute
            self.values = values
        }
    }

    // MARK: CatalogCreateCatalogItemRequestVariant convenience initializers and mutators

    public extension CatalogCreateCatalogItemRequestVariant {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogCreateCatalogItemRequestVariant.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            attribute: String? = nil,
            values: [String]? = nil
        ) -> CatalogCreateCatalogItemRequestVariant {
            return CatalogCreateCatalogItemRequestVariant(
                attribute: attribute ?? self.attribute,
                values: values ?? self.values
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogErrorResponse
    public struct CatalogErrorResponse: Codable, Equatable {
        public let code: String?
        public let error: String

        public init(code: String?, error: String) {
            self.code = code
            self.error = error
        }
    }

    // MARK: CatalogErrorResponse convenience initializers and mutators

    public extension CatalogErrorResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogErrorResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            code: String?? = nil,
            error: String? = nil
        ) -> CatalogErrorResponse {
            return CatalogErrorResponse(
                code: code ?? self.code,
                error: error ?? self.error
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogUpdateCatalogItemRequest
    public struct CatalogUpdateCatalogItemRequest: Codable, Equatable {
        public let availability: Availability?
        public let brand: String?
        public let categories: [String]?
        public let color, condition, currency, description: String?
        public let faqs: [CatalogUpdateCatalogItemRequestFAQ]?
        public let images: [String]?
        public let links: [CatalogUpdateCatalogItemRequestLink]?
        public let material, model: String?
        public let name: String?
        public let price: Double?
        public let productSku, productURL: String?
        public let qas: [CatalogUpdateCatalogItemRequestQA]?
        public let ratingValue: Double?
        public let reviewCount: Double?
        public let reviews: [CatalogUpdateCatalogItemRequestReview]?
        public let seller, size, sku: String?
        public let techs: [String: String]?
        public let variants: [CatalogUpdateCatalogItemRequestVariant]?
        public let weight: Double?
        public let weightUnit: WeightUnit?

        public enum CodingKeys: String, CodingKey {
            case availability, brand, categories, color, condition, currency, description, faqs, images, links, material, model, name, price, productSku
            case productURL = "productUrl"
            case qas, ratingValue, reviewCount, reviews, seller, size, sku, techs, variants, weight, weightUnit
        }

        public init(availability: Availability?, brand: String?, categories: [String]?, color: String?, condition: String?, currency: String?, description: String?, faqs: [CatalogUpdateCatalogItemRequestFAQ]?, images: [String]?, links: [CatalogUpdateCatalogItemRequestLink]?, material: String?, model: String?, name: String?, price: Double?, productSku: String?, productURL: String?, qas: [CatalogUpdateCatalogItemRequestQA]?, ratingValue: Double?, reviewCount: Double?, reviews: [CatalogUpdateCatalogItemRequestReview]?, seller: String?, size: String?, sku: String?, techs: [String: String]?, variants: [CatalogUpdateCatalogItemRequestVariant]?, weight: Double?, weightUnit: WeightUnit?) {
            self.availability = availability
            self.brand = brand
            self.categories = categories
            self.color = color
            self.condition = condition
            self.currency = currency
            self.description = description
            self.faqs = faqs
            self.images = images
            self.links = links
            self.material = material
            self.model = model
            self.name = name
            self.price = price
            self.productSku = productSku
            self.productURL = productURL
            self.qas = qas
            self.ratingValue = ratingValue
            self.reviewCount = reviewCount
            self.reviews = reviews
            self.seller = seller
            self.size = size
            self.sku = sku
            self.techs = techs
            self.variants = variants
            self.weight = weight
            self.weightUnit = weightUnit
        }
    }

    // MARK: CatalogUpdateCatalogItemRequest convenience initializers and mutators

    public extension CatalogUpdateCatalogItemRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogUpdateCatalogItemRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            availability: Availability?? = nil,
            brand: String?? = nil,
            categories: [String]?? = nil,
            color: String?? = nil,
            condition: String?? = nil,
            currency: String?? = nil,
            description: String?? = nil,
            faqs: [CatalogUpdateCatalogItemRequestFAQ]?? = nil,
            images: [String]?? = nil,
            links: [CatalogUpdateCatalogItemRequestLink]?? = nil,
            material: String?? = nil,
            model: String?? = nil,
            name: String?? = nil,
            price: Double?? = nil,
            productSku: String?? = nil,
            productURL: String?? = nil,
            qas: [CatalogUpdateCatalogItemRequestQA]?? = nil,
            ratingValue: Double?? = nil,
            reviewCount: Double?? = nil,
            reviews: [CatalogUpdateCatalogItemRequestReview]?? = nil,
            seller: String?? = nil,
            size: String?? = nil,
            sku: String?? = nil,
            techs: [String: String]?? = nil,
            variants: [CatalogUpdateCatalogItemRequestVariant]?? = nil,
            weight: Double?? = nil,
            weightUnit: WeightUnit?? = nil
        ) -> CatalogUpdateCatalogItemRequest {
            return CatalogUpdateCatalogItemRequest(
                availability: availability ?? self.availability,
                brand: brand ?? self.brand,
                categories: categories ?? self.categories,
                color: color ?? self.color,
                condition: condition ?? self.condition,
                currency: currency ?? self.currency,
                description: description ?? self.description,
                faqs: faqs ?? self.faqs,
                images: images ?? self.images,
                links: links ?? self.links,
                material: material ?? self.material,
                model: model ?? self.model,
                name: name ?? self.name,
                price: price ?? self.price,
                productSku: productSku ?? self.productSku,
                productURL: productURL ?? self.productURL,
                qas: qas ?? self.qas,
                ratingValue: ratingValue ?? self.ratingValue,
                reviewCount: reviewCount ?? self.reviewCount,
                reviews: reviews ?? self.reviews,
                seller: seller ?? self.seller,
                size: size ?? self.size,
                sku: sku ?? self.sku,
                techs: techs ?? self.techs,
                variants: variants ?? self.variants,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogUpdateCatalogItemRequestFAQ
    public struct CatalogUpdateCatalogItemRequestFAQ: Codable, Equatable {
        public let answer, question: String

        public init(answer: String, question: String) {
            self.answer = answer
            self.question = question
        }
    }

    // MARK: CatalogUpdateCatalogItemRequestFAQ convenience initializers and mutators

    public extension CatalogUpdateCatalogItemRequestFAQ {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogUpdateCatalogItemRequestFAQ.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answer: String? = nil,
            question: String? = nil
        ) -> CatalogUpdateCatalogItemRequestFAQ {
            return CatalogUpdateCatalogItemRequestFAQ(
                answer: answer ?? self.answer,
                question: question ?? self.question
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogUpdateCatalogItemRequestLink
    public struct CatalogUpdateCatalogItemRequestLink: Codable, Equatable {
        public let title, url: String

        public init(title: String, url: String) {
            self.title = title
            self.url = url
        }
    }

    // MARK: CatalogUpdateCatalogItemRequestLink convenience initializers and mutators

    public extension CatalogUpdateCatalogItemRequestLink {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogUpdateCatalogItemRequestLink.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            title: String? = nil,
            url: String? = nil
        ) -> CatalogUpdateCatalogItemRequestLink {
            return CatalogUpdateCatalogItemRequestLink(
                title: title ?? self.title,
                url: url ?? self.url
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogUpdateCatalogItemRequestQA
    public struct CatalogUpdateCatalogItemRequestQA: Codable, Equatable {
        public let answers: [StickyAnswer]
        public let date, question: String
        public let user: String?

        public init(answers: [StickyAnswer], date: String, question: String, user: String?) {
            self.answers = answers
            self.date = date
            self.question = question
            self.user = user
        }
    }

    // MARK: CatalogUpdateCatalogItemRequestQA convenience initializers and mutators

    public extension CatalogUpdateCatalogItemRequestQA {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogUpdateCatalogItemRequestQA.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            answers: [StickyAnswer]? = nil,
            date: String? = nil,
            question: String? = nil,
            user: String?? = nil
        ) -> CatalogUpdateCatalogItemRequestQA {
            return CatalogUpdateCatalogItemRequestQA(
                answers: answers ?? self.answers,
                date: date ?? self.date,
                question: question ?? self.question,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - StickyAnswer
    public struct StickyAnswer: Codable, Equatable {
        public let a, date: String
        public let upvotes: Double?
        public let user: String?

        public init(a: String, date: String, upvotes: Double?, user: String?) {
            self.a = a
            self.date = date
            self.upvotes = upvotes
            self.user = user
        }
    }

    // MARK: StickyAnswer convenience initializers and mutators

    public extension StickyAnswer {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(StickyAnswer.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            a: String? = nil,
            date: String? = nil,
            upvotes: Double?? = nil,
            user: String?? = nil
        ) -> StickyAnswer {
            return StickyAnswer(
                a: a ?? self.a,
                date: date ?? self.date,
                upvotes: upvotes ?? self.upvotes,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogUpdateCatalogItemRequestReview
    public struct CatalogUpdateCatalogItemRequestReview: Codable, Equatable {
        public let context: [String: String]?
        public let date: String
        public let downvotes: Double?
        public let images: [String]?
        public let rating: Double
        public let recommends: Bool?
        public let text, title: String
        public let upvotes: Double?
        public let userAvatar: String?
        public let userName: String
        public let verified: Bool?

        public enum CodingKeys: String, CodingKey {
            case context, date, downvotes, images, rating, recommends, text, title, upvotes
            case userAvatar = "user_avatar"
            case userName = "user_name"
            case verified
        }

        public init(context: [String: String]?, date: String, downvotes: Double?, images: [String]?, rating: Double, recommends: Bool?, text: String, title: String, upvotes: Double?, userAvatar: String?, userName: String, verified: Bool?) {
            self.context = context
            self.date = date
            self.downvotes = downvotes
            self.images = images
            self.rating = rating
            self.recommends = recommends
            self.text = text
            self.title = title
            self.upvotes = upvotes
            self.userAvatar = userAvatar
            self.userName = userName
            self.verified = verified
        }
    }

    // MARK: CatalogUpdateCatalogItemRequestReview convenience initializers and mutators

    public extension CatalogUpdateCatalogItemRequestReview {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogUpdateCatalogItemRequestReview.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            context: [String: String]?? = nil,
            date: String? = nil,
            downvotes: Double?? = nil,
            images: [String]?? = nil,
            rating: Double? = nil,
            recommends: Bool?? = nil,
            text: String? = nil,
            title: String? = nil,
            upvotes: Double?? = nil,
            userAvatar: String?? = nil,
            userName: String? = nil,
            verified: Bool?? = nil
        ) -> CatalogUpdateCatalogItemRequestReview {
            return CatalogUpdateCatalogItemRequestReview(
                context: context ?? self.context,
                date: date ?? self.date,
                downvotes: downvotes ?? self.downvotes,
                images: images ?? self.images,
                rating: rating ?? self.rating,
                recommends: recommends ?? self.recommends,
                text: text ?? self.text,
                title: title ?? self.title,
                upvotes: upvotes ?? self.upvotes,
                userAvatar: userAvatar ?? self.userAvatar,
                userName: userName ?? self.userName,
                verified: verified ?? self.verified
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CatalogUpdateCatalogItemRequestVariant
    public struct CatalogUpdateCatalogItemRequestVariant: Codable, Equatable {
        public let attribute: String
        public let values: [String]

        public init(attribute: String, values: [String]) {
            self.attribute = attribute
            self.values = values
        }
    }

    // MARK: CatalogUpdateCatalogItemRequestVariant convenience initializers and mutators

    public extension CatalogUpdateCatalogItemRequestVariant {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CatalogUpdateCatalogItemRequestVariant.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            attribute: String? = nil,
            values: [String]? = nil
        ) -> CatalogUpdateCatalogItemRequestVariant {
            return CatalogUpdateCatalogItemRequestVariant(
                attribute: attribute ?? self.attribute,
                values: values ?? self.values
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - ChatCreateReportRequest
    public struct ChatCreateReportRequest: Codable, Equatable {
        public let aiResponse, reason: String
        public let userComment: String?
        public let userQuery: String

        public init(aiResponse: String, reason: String, userComment: String?, userQuery: String) {
            self.aiResponse = aiResponse
            self.reason = reason
            self.userComment = userComment
            self.userQuery = userQuery
        }
    }

    // MARK: ChatCreateReportRequest convenience initializers and mutators

    public extension ChatCreateReportRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(ChatCreateReportRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            aiResponse: String? = nil,
            reason: String? = nil,
            userComment: String?? = nil,
            userQuery: String? = nil
        ) -> ChatCreateReportRequest {
            return ChatCreateReportRequest(
                aiResponse: aiResponse ?? self.aiResponse,
                reason: reason ?? self.reason,
                userComment: userComment ?? self.userComment,
                userQuery: userQuery ?? self.userQuery
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - ChatUpdateReportStatusRequest
    public struct ChatUpdateReportStatusRequest: Codable, Equatable {
        public let status: String

        public init(status: String) {
            self.status = status
        }
    }

    // MARK: ChatUpdateReportStatusRequest convenience initializers and mutators

    public extension ChatUpdateReportStatusRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(ChatUpdateReportStatusRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            status: String? = nil
        ) -> ChatUpdateReportStatusRequest {
            return ChatUpdateReportStatusRequest(
                status: status ?? self.status
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - FeedCreateCommentRequest
    public struct FeedCreateCommentRequest: Codable, Equatable {
        public let content: String
        public let parentCommentID: Int?

        public enum CodingKeys: String, CodingKey {
            case content
            case parentCommentID = "parentCommentId"
        }

        public init(content: String, parentCommentID: Int?) {
            self.content = content
            self.parentCommentID = parentCommentID
        }
    }

    // MARK: FeedCreateCommentRequest convenience initializers and mutators

    public extension FeedCreateCommentRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(FeedCreateCommentRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            content: String? = nil,
            parentCommentID: Int?? = nil
        ) -> FeedCreateCommentRequest {
            return FeedCreateCommentRequest(
                content: content ?? self.content,
                parentCommentID: parentCommentID ?? self.parentCommentID
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - FeedCreatePostRequest
    public struct FeedCreatePostRequest: Codable, Equatable {
        public let caption: String?
        public let images: [String]

        public init(caption: String?, images: [String]) {
            self.caption = caption
            self.images = images
        }
    }

    // MARK: FeedCreatePostRequest convenience initializers and mutators

    public extension FeedCreatePostRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(FeedCreatePostRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            caption: String?? = nil,
            images: [String]? = nil
        ) -> FeedCreatePostRequest {
            return FeedCreatePostRequest(
                caption: caption ?? self.caption,
                images: images ?? self.images
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - FeedFeedResponse
    public struct FeedFeedResponse: Codable, Equatable {
        public let items: [FeedFeedResponseItem]
        public let limit, page, total, totalPages: Int

        public init(items: [FeedFeedResponseItem], limit: Int, page: Int, total: Int, totalPages: Int) {
            self.items = items
            self.limit = limit
            self.page = page
            self.total = total
            self.totalPages = totalPages
        }
    }

    // MARK: FeedFeedResponse convenience initializers and mutators

    public extension FeedFeedResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(FeedFeedResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            items: [FeedFeedResponseItem]? = nil,
            limit: Int? = nil,
            page: Int? = nil,
            total: Int? = nil,
            totalPages: Int? = nil
        ) -> FeedFeedResponse {
            return FeedFeedResponse(
                items: items ?? self.items,
                limit: limit ?? self.limit,
                page: page ?? self.page,
                total: total ?? self.total,
                totalPages: totalPages ?? self.totalPages
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - FeedFeedResponseItem
    public struct FeedFeedResponseItem: Codable, Equatable {
        public let author: Author?
        public let caption: String
        public let commentCount: Int
        public let createdAt: Date
        public let id: Int
        public let images: [String]
        public let likeCount: Int
        public let likedByMe: Bool
        public let updatedAt: Date
        public let userID: String

        public enum CodingKeys: String, CodingKey {
            case author, caption, commentCount, createdAt, id, images, likeCount, likedByMe, updatedAt
            case userID = "userId"
        }

        public init(author: Author?, caption: String, commentCount: Int, createdAt: Date, id: Int, images: [String], likeCount: Int, likedByMe: Bool, updatedAt: Date, userID: String) {
            self.author = author
            self.caption = caption
            self.commentCount = commentCount
            self.createdAt = createdAt
            self.id = id
            self.images = images
            self.likeCount = likeCount
            self.likedByMe = likedByMe
            self.updatedAt = updatedAt
            self.userID = userID
        }
    }

    // MARK: FeedFeedResponseItem convenience initializers and mutators

    public extension FeedFeedResponseItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(FeedFeedResponseItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            author: Author?? = nil,
            caption: String? = nil,
            commentCount: Int? = nil,
            createdAt: Date? = nil,
            id: Int? = nil,
            images: [String]? = nil,
            likeCount: Int? = nil,
            likedByMe: Bool? = nil,
            updatedAt: Date? = nil,
            userID: String? = nil
        ) -> FeedFeedResponseItem {
            return FeedFeedResponseItem(
                author: author ?? self.author,
                caption: caption ?? self.caption,
                commentCount: commentCount ?? self.commentCount,
                createdAt: createdAt ?? self.createdAt,
                id: id ?? self.id,
                images: images ?? self.images,
                likeCount: likeCount ?? self.likeCount,
                likedByMe: likedByMe ?? self.likedByMe,
                updatedAt: updatedAt ?? self.updatedAt,
                userID: userID ?? self.userID
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Author
    public struct Author: Codable, Equatable {
        public let firstName, id, lastName: String

        public init(firstName: String, id: String, lastName: String) {
            self.firstName = firstName
            self.id = id
            self.lastName = lastName
        }
    }

    // MARK: Author convenience initializers and mutators

    public extension Author {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Author.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            firstName: String? = nil,
            id: String? = nil,
            lastName: String? = nil
        ) -> Author {
            return Author(
                firstName: firstName ?? self.firstName,
                id: id ?? self.id,
                lastName: lastName ?? self.lastName
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - GuidesGuideCategoriesResponse
    public struct GuidesGuideCategoriesResponse: Codable, Equatable {
        public let categories: [String]
        public let count: Int

        public init(categories: [String], count: Int) {
            self.categories = categories
            self.count = count
        }
    }

    // MARK: GuidesGuideCategoriesResponse convenience initializers and mutators

    public extension GuidesGuideCategoriesResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(GuidesGuideCategoriesResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            categories: [String]? = nil,
            count: Int? = nil
        ) -> GuidesGuideCategoriesResponse {
            return GuidesGuideCategoriesResponse(
                categories: categories ?? self.categories,
                count: count ?? self.count
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - GuidesGuideDetail
    public struct GuidesGuideDetail: Codable, Equatable {
        public let author: String?
        public let categories: [String]?
        public let category, content: String
        public let createdAt: Date
        public let description: String
        public let difficulty: String?
        public let id, key: String
        public let readingTime: Double?
        public let title: String
        public let updatedAt: Date

        public init(author: String?, categories: [String]?, category: String, content: String, createdAt: Date, description: String, difficulty: String?, id: String, key: String, readingTime: Double?, title: String, updatedAt: Date) {
            self.author = author
            self.categories = categories
            self.category = category
            self.content = content
            self.createdAt = createdAt
            self.description = description
            self.difficulty = difficulty
            self.id = id
            self.key = key
            self.readingTime = readingTime
            self.title = title
            self.updatedAt = updatedAt
        }
    }

    // MARK: GuidesGuideDetail convenience initializers and mutators

    public extension GuidesGuideDetail {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(GuidesGuideDetail.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            author: String?? = nil,
            categories: [String]?? = nil,
            category: String? = nil,
            content: String? = nil,
            createdAt: Date? = nil,
            description: String? = nil,
            difficulty: String?? = nil,
            id: String? = nil,
            key: String? = nil,
            readingTime: Double?? = nil,
            title: String? = nil,
            updatedAt: Date? = nil
        ) -> GuidesGuideDetail {
            return GuidesGuideDetail(
                author: author ?? self.author,
                categories: categories ?? self.categories,
                category: category ?? self.category,
                content: content ?? self.content,
                createdAt: createdAt ?? self.createdAt,
                description: description ?? self.description,
                difficulty: difficulty ?? self.difficulty,
                id: id ?? self.id,
                key: key ?? self.key,
                readingTime: readingTime ?? self.readingTime,
                title: title ?? self.title,
                updatedAt: updatedAt ?? self.updatedAt
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - GuidesGuideSearchResponse
    public struct GuidesGuideSearchResponse: Codable, Equatable {
        public let items: [GuidesGuideSearchResponseItem]
        public let limit, page: Double
        public let query: String
        public let totalCount, totalPages: Double

        public init(items: [GuidesGuideSearchResponseItem], limit: Double, page: Double, query: String, totalCount: Double, totalPages: Double) {
            self.items = items
            self.limit = limit
            self.page = page
            self.query = query
            self.totalCount = totalCount
            self.totalPages = totalPages
        }
    }

    // MARK: GuidesGuideSearchResponse convenience initializers and mutators

    public extension GuidesGuideSearchResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(GuidesGuideSearchResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            items: [GuidesGuideSearchResponseItem]? = nil,
            limit: Double? = nil,
            page: Double? = nil,
            query: String? = nil,
            totalCount: Double? = nil,
            totalPages: Double? = nil
        ) -> GuidesGuideSearchResponse {
            return GuidesGuideSearchResponse(
                items: items ?? self.items,
                limit: limit ?? self.limit,
                page: page ?? self.page,
                query: query ?? self.query,
                totalCount: totalCount ?? self.totalCount,
                totalPages: totalPages ?? self.totalPages
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - GuidesGuideSearchResponseItem
    public struct GuidesGuideSearchResponseItem: Codable, Equatable {
        public let author: String?
        public let categories: [String]?
        public let category: String
        public let content: String?
        public let createdAt: Date
        public let description: String
        public let difficulty: String?
        public let id, key: String
        public let readingTime: Double?
        public let title: String
        public let updatedAt: Date

        public init(author: String?, categories: [String]?, category: String, content: String?, createdAt: Date, description: String, difficulty: String?, id: String, key: String, readingTime: Double?, title: String, updatedAt: Date) {
            self.author = author
            self.categories = categories
            self.category = category
            self.content = content
            self.createdAt = createdAt
            self.description = description
            self.difficulty = difficulty
            self.id = id
            self.key = key
            self.readingTime = readingTime
            self.title = title
            self.updatedAt = updatedAt
        }
    }

    // MARK: GuidesGuideSearchResponseItem convenience initializers and mutators

    public extension GuidesGuideSearchResponseItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(GuidesGuideSearchResponseItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            author: String?? = nil,
            categories: [String]?? = nil,
            category: String? = nil,
            content: String?? = nil,
            createdAt: Date? = nil,
            description: String? = nil,
            difficulty: String?? = nil,
            id: String? = nil,
            key: String? = nil,
            readingTime: Double?? = nil,
            title: String? = nil,
            updatedAt: Date? = nil
        ) -> GuidesGuideSearchResponseItem {
            return GuidesGuideSearchResponseItem(
                author: author ?? self.author,
                categories: categories ?? self.categories,
                category: category ?? self.category,
                content: content ?? self.content,
                createdAt: createdAt ?? self.createdAt,
                description: description ?? self.description,
                difficulty: difficulty ?? self.difficulty,
                id: id ?? self.id,
                key: key ?? self.key,
                readingTime: readingTime ?? self.readingTime,
                title: title ?? self.title,
                updatedAt: updatedAt ?? self.updatedAt
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - GuidesGuidesResponse
    public struct GuidesGuidesResponse: Codable, Equatable {
        public let items: [GuidesGuidesResponseItem]
        public let limit, page, totalCount, totalPages: Double

        public init(items: [GuidesGuidesResponseItem], limit: Double, page: Double, totalCount: Double, totalPages: Double) {
            self.items = items
            self.limit = limit
            self.page = page
            self.totalCount = totalCount
            self.totalPages = totalPages
        }
    }

    // MARK: GuidesGuidesResponse convenience initializers and mutators

    public extension GuidesGuidesResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(GuidesGuidesResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            items: [GuidesGuidesResponseItem]? = nil,
            limit: Double? = nil,
            page: Double? = nil,
            totalCount: Double? = nil,
            totalPages: Double? = nil
        ) -> GuidesGuidesResponse {
            return GuidesGuidesResponse(
                items: items ?? self.items,
                limit: limit ?? self.limit,
                page: page ?? self.page,
                totalCount: totalCount ?? self.totalCount,
                totalPages: totalPages ?? self.totalPages
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - GuidesGuidesResponseItem
    public struct GuidesGuidesResponseItem: Codable, Equatable {
        public let author: String?
        public let categories: [String]?
        public let category: String
        public let content: String?
        public let createdAt: Date
        public let description: String
        public let difficulty: String?
        public let id, key: String
        public let readingTime: Double?
        public let title: String
        public let updatedAt: Date

        public init(author: String?, categories: [String]?, category: String, content: String?, createdAt: Date, description: String, difficulty: String?, id: String, key: String, readingTime: Double?, title: String, updatedAt: Date) {
            self.author = author
            self.categories = categories
            self.category = category
            self.content = content
            self.createdAt = createdAt
            self.description = description
            self.difficulty = difficulty
            self.id = id
            self.key = key
            self.readingTime = readingTime
            self.title = title
            self.updatedAt = updatedAt
        }
    }

    // MARK: GuidesGuidesResponseItem convenience initializers and mutators

    public extension GuidesGuidesResponseItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(GuidesGuidesResponseItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            author: String?? = nil,
            categories: [String]?? = nil,
            category: String? = nil,
            content: String?? = nil,
            createdAt: Date? = nil,
            description: String? = nil,
            difficulty: String?? = nil,
            id: String? = nil,
            key: String? = nil,
            readingTime: Double?? = nil,
            title: String? = nil,
            updatedAt: Date? = nil
        ) -> GuidesGuidesResponseItem {
            return GuidesGuidesResponseItem(
                author: author ?? self.author,
                categories: categories ?? self.categories,
                category: category ?? self.category,
                content: content ?? self.content,
                createdAt: createdAt ?? self.createdAt,
                description: description ?? self.description,
                difficulty: difficulty ?? self.difficulty,
                id: id ?? self.id,
                key: key ?? self.key,
                readingTime: readingTime ?? self.readingTime,
                title: title ?? self.title,
                updatedAt: updatedAt ?? self.updatedAt
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PackTemplatesAIPackAnalysis
    public struct PackTemplatesAIPackAnalysis: Codable, Equatable {
        public let items: [PackTemplatesAIPackAnalysisItem]
        public let templateCategory: Category
        public let templateDescription, templateName: String

        public init(items: [PackTemplatesAIPackAnalysisItem], templateCategory: Category, templateDescription: String, templateName: String) {
            self.items = items
            self.templateCategory = templateCategory
            self.templateDescription = templateDescription
            self.templateName = templateName
        }
    }

    // MARK: PackTemplatesAIPackAnalysis convenience initializers and mutators

    public extension PackTemplatesAIPackAnalysis {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackTemplatesAIPackAnalysis.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            items: [PackTemplatesAIPackAnalysisItem]? = nil,
            templateCategory: Category? = nil,
            templateDescription: String? = nil,
            templateName: String? = nil
        ) -> PackTemplatesAIPackAnalysis {
            return PackTemplatesAIPackAnalysis(
                items: items ?? self.items,
                templateCategory: templateCategory ?? self.templateCategory,
                templateDescription: templateDescription ?? self.templateDescription,
                templateName: templateName ?? self.templateName
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PackTemplatesAIPackAnalysisItem
    public struct PackTemplatesAIPackAnalysisItem: Codable, Equatable {
        public let category: String
        public let consumable: Bool?
        public let description, name: String
        public let quantity: Int?
        public let weightGrams: Double?
        public let worn: Bool?

        public init(category: String, consumable: Bool?, description: String, name: String, quantity: Int?, weightGrams: Double?, worn: Bool?) {
            self.category = category
            self.consumable = consumable
            self.description = description
            self.name = name
            self.quantity = quantity
            self.weightGrams = weightGrams
            self.worn = worn
        }
    }

    // MARK: PackTemplatesAIPackAnalysisItem convenience initializers and mutators

    public extension PackTemplatesAIPackAnalysisItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackTemplatesAIPackAnalysisItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            category: String? = nil,
            consumable: Bool?? = nil,
            description: String? = nil,
            name: String? = nil,
            quantity: Int?? = nil,
            weightGrams: Double?? = nil,
            worn: Bool?? = nil
        ) -> PackTemplatesAIPackAnalysisItem {
            return PackTemplatesAIPackAnalysisItem(
                category: category ?? self.category,
                consumable: consumable ?? self.consumable,
                description: description ?? self.description,
                name: name ?? self.name,
                quantity: quantity ?? self.quantity,
                weightGrams: weightGrams ?? self.weightGrams,
                worn: worn ?? self.worn
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    public enum Category: String, Codable, Equatable {
        case backpacking = "backpacking"
        case camping = "camping"
        case climbing = "climbing"
        case custom = "custom"
        case desert = "desert"
        case hiking = "hiking"
        case skiing = "skiing"
        case waterSports = "water sports"
        case winter = "winter"
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PackTemplatesCreatePackTemplateItemRequest
    public struct PackTemplatesCreatePackTemplateItemRequest: Codable, Equatable {
        public let category: String?
        public let consumable: Bool?
        public let description: String?
        public let id: String
        public let image: String?
        public let name: String
        public let notes: String?
        public let quantity: Int?
        public let weight: Double
        public let weightUnit: WeightUnit
        public let worn: Bool?

        public init(category: String?, consumable: Bool?, description: String?, id: String, image: String?, name: String, notes: String?, quantity: Int?, weight: Double, weightUnit: WeightUnit, worn: Bool?) {
            self.category = category
            self.consumable = consumable
            self.description = description
            self.id = id
            self.image = image
            self.name = name
            self.notes = notes
            self.quantity = quantity
            self.weight = weight
            self.weightUnit = weightUnit
            self.worn = worn
        }
    }

    // MARK: PackTemplatesCreatePackTemplateItemRequest convenience initializers and mutators

    public extension PackTemplatesCreatePackTemplateItemRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackTemplatesCreatePackTemplateItemRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            category: String?? = nil,
            consumable: Bool?? = nil,
            description: String?? = nil,
            id: String? = nil,
            image: String?? = nil,
            name: String? = nil,
            notes: String?? = nil,
            quantity: Int?? = nil,
            weight: Double? = nil,
            weightUnit: WeightUnit? = nil,
            worn: Bool?? = nil
        ) -> PackTemplatesCreatePackTemplateItemRequest {
            return PackTemplatesCreatePackTemplateItemRequest(
                category: category ?? self.category,
                consumable: consumable ?? self.consumable,
                description: description ?? self.description,
                id: id ?? self.id,
                image: image ?? self.image,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                quantity: quantity ?? self.quantity,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit,
                worn: worn ?? self.worn
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PackTemplatesCreatePackTemplateRequest
    public struct PackTemplatesCreatePackTemplateRequest: Codable, Equatable {
        public let category: String
        public let description: String?
        public let id: String
        public let image: String?
        public let isAppTemplate: Bool?
        public let localCreatedAt, localUpdatedAt: Date
        public let name: String
        public let tags: [String]?

        public init(category: String, description: String?, id: String, image: String?, isAppTemplate: Bool?, localCreatedAt: Date, localUpdatedAt: Date, name: String, tags: [String]?) {
            self.category = category
            self.description = description
            self.id = id
            self.image = image
            self.isAppTemplate = isAppTemplate
            self.localCreatedAt = localCreatedAt
            self.localUpdatedAt = localUpdatedAt
            self.name = name
            self.tags = tags
        }
    }

    // MARK: PackTemplatesCreatePackTemplateRequest convenience initializers and mutators

    public extension PackTemplatesCreatePackTemplateRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackTemplatesCreatePackTemplateRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            category: String? = nil,
            description: String?? = nil,
            id: String? = nil,
            image: String?? = nil,
            isAppTemplate: Bool?? = nil,
            localCreatedAt: Date? = nil,
            localUpdatedAt: Date? = nil,
            name: String? = nil,
            tags: [String]?? = nil
        ) -> PackTemplatesCreatePackTemplateRequest {
            return PackTemplatesCreatePackTemplateRequest(
                category: category ?? self.category,
                description: description ?? self.description,
                id: id ?? self.id,
                image: image ?? self.image,
                isAppTemplate: isAppTemplate ?? self.isAppTemplate,
                localCreatedAt: localCreatedAt ?? self.localCreatedAt,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                name: name ?? self.name,
                tags: tags ?? self.tags
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PackTemplatesGenerateFromOnlineContentRequest
    public struct PackTemplatesGenerateFromOnlineContentRequest: Codable, Equatable {
        public let contentURL: String
        public let isAppTemplate: Bool?

        public enum CodingKeys: String, CodingKey {
            case contentURL = "contentUrl"
            case isAppTemplate
        }

        public init(contentURL: String, isAppTemplate: Bool?) {
            self.contentURL = contentURL
            self.isAppTemplate = isAppTemplate
        }
    }

    // MARK: PackTemplatesGenerateFromOnlineContentRequest convenience initializers and mutators

    public extension PackTemplatesGenerateFromOnlineContentRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackTemplatesGenerateFromOnlineContentRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            contentURL: String? = nil,
            isAppTemplate: Bool?? = nil
        ) -> PackTemplatesGenerateFromOnlineContentRequest {
            return PackTemplatesGenerateFromOnlineContentRequest(
                contentURL: contentURL ?? self.contentURL,
                isAppTemplate: isAppTemplate ?? self.isAppTemplate
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PackTemplatesUpdatePackTemplateItemRequest
    public struct PackTemplatesUpdatePackTemplateItemRequest: Codable, Equatable {
        public let category: String?
        public let consumable, deleted: Bool?
        public let description, image: String?
        public let name: String?
        public let notes: String?
        public let quantity: Int?
        public let weight: Double?
        public let weightUnit: WeightUnit?
        public let worn: Bool?

        public init(category: String?, consumable: Bool?, deleted: Bool?, description: String?, image: String?, name: String?, notes: String?, quantity: Int?, weight: Double?, weightUnit: WeightUnit?, worn: Bool?) {
            self.category = category
            self.consumable = consumable
            self.deleted = deleted
            self.description = description
            self.image = image
            self.name = name
            self.notes = notes
            self.quantity = quantity
            self.weight = weight
            self.weightUnit = weightUnit
            self.worn = worn
        }
    }

    // MARK: PackTemplatesUpdatePackTemplateItemRequest convenience initializers and mutators

    public extension PackTemplatesUpdatePackTemplateItemRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackTemplatesUpdatePackTemplateItemRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            category: String?? = nil,
            consumable: Bool?? = nil,
            deleted: Bool?? = nil,
            description: String?? = nil,
            image: String?? = nil,
            name: String?? = nil,
            notes: String?? = nil,
            quantity: Int?? = nil,
            weight: Double?? = nil,
            weightUnit: WeightUnit?? = nil,
            worn: Bool?? = nil
        ) -> PackTemplatesUpdatePackTemplateItemRequest {
            return PackTemplatesUpdatePackTemplateItemRequest(
                category: category ?? self.category,
                consumable: consumable ?? self.consumable,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                image: image ?? self.image,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                quantity: quantity ?? self.quantity,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit,
                worn: worn ?? self.worn
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PackTemplatesUpdatePackTemplateRequest
    public struct PackTemplatesUpdatePackTemplateRequest: Codable, Equatable {
        public let category: String?
        public let deleted: Bool?
        public let description, image: String
        public let isAppTemplate: Bool?
        public let localUpdatedAt: Date?
        public let name: String?
        public let tags: [String]

        public init(category: String?, deleted: Bool?, description: String, image: String, isAppTemplate: Bool?, localUpdatedAt: Date?, name: String?, tags: [String]) {
            self.category = category
            self.deleted = deleted
            self.description = description
            self.image = image
            self.isAppTemplate = isAppTemplate
            self.localUpdatedAt = localUpdatedAt
            self.name = name
            self.tags = tags
        }
    }

    // MARK: PackTemplatesUpdatePackTemplateRequest convenience initializers and mutators

    public extension PackTemplatesUpdatePackTemplateRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PackTemplatesUpdatePackTemplateRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            category: String?? = nil,
            deleted: Bool?? = nil,
            description: String? = nil,
            image: String? = nil,
            isAppTemplate: Bool?? = nil,
            localUpdatedAt: Date?? = nil,
            name: String?? = nil,
            tags: [String]? = nil
        ) -> PackTemplatesUpdatePackTemplateRequest {
            return PackTemplatesUpdatePackTemplateRequest(
                category: category ?? self.category,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                image: image ?? self.image,
                isAppTemplate: isAppTemplate ?? self.isAppTemplate,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                name: name ?? self.name,
                tags: tags ?? self.tags
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksAddPackItemBody
    public struct PacksAddPackItemBody: Codable, Equatable {
        public let catalogItemID: Int?
        public let category: String?
        public let consumable: Bool?
        public let description: String?
        public let id: String
        public let image: String?
        public let name: String
        public let notes: String?
        public let quantity: Int?
        public let weight: Double
        public let weightUnit: WeightUnit?
        public let worn: Bool?

        public enum CodingKeys: String, CodingKey {
            case catalogItemID = "catalogItemId"
            case category, consumable, description, id, image, name, notes, quantity, weight, weightUnit, worn
        }

        public init(catalogItemID: Int?, category: String?, consumable: Bool?, description: String?, id: String, image: String?, name: String, notes: String?, quantity: Int?, weight: Double, weightUnit: WeightUnit?, worn: Bool?) {
            self.catalogItemID = catalogItemID
            self.category = category
            self.consumable = consumable
            self.description = description
            self.id = id
            self.image = image
            self.name = name
            self.notes = notes
            self.quantity = quantity
            self.weight = weight
            self.weightUnit = weightUnit
            self.worn = worn
        }
    }

    // MARK: PacksAddPackItemBody convenience initializers and mutators

    public extension PacksAddPackItemBody {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksAddPackItemBody.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            catalogItemID: Int?? = nil,
            category: String?? = nil,
            consumable: Bool?? = nil,
            description: String?? = nil,
            id: String? = nil,
            image: String?? = nil,
            name: String? = nil,
            notes: String?? = nil,
            quantity: Int?? = nil,
            weight: Double? = nil,
            weightUnit: WeightUnit?? = nil,
            worn: Bool?? = nil
        ) -> PacksAddPackItemBody {
            return PacksAddPackItemBody(
                catalogItemID: catalogItemID ?? self.catalogItemID,
                category: category ?? self.category,
                consumable: consumable ?? self.consumable,
                description: description ?? self.description,
                id: id ?? self.id,
                image: image ?? self.image,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                quantity: quantity ?? self.quantity,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit,
                worn: worn ?? self.worn
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksAnalyzeImageRequest
    public struct PacksAnalyzeImageRequest: Codable, Equatable {
        public let image: String
        public let matchLimit: Int?

        public init(image: String, matchLimit: Int?) {
            self.image = image
            self.matchLimit = matchLimit
        }
    }

    // MARK: PacksAnalyzeImageRequest convenience initializers and mutators

    public extension PacksAnalyzeImageRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksAnalyzeImageRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            image: String? = nil,
            matchLimit: Int?? = nil
        ) -> PacksAnalyzeImageRequest {
            return PacksAnalyzeImageRequest(
                image: image ?? self.image,
                matchLimit: matchLimit ?? self.matchLimit
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksCreatePackBody
    public struct PacksCreatePackBody: Codable, Equatable {
        public let category, description: String?
        public let id: String
        public let image: String?
        public let isPublic: Bool?
        public let localCreatedAt, localUpdatedAt: Date
        public let name: String
        public let tags: [String]?

        public init(category: String?, description: String?, id: String, image: String?, isPublic: Bool?, localCreatedAt: Date, localUpdatedAt: Date, name: String, tags: [String]?) {
            self.category = category
            self.description = description
            self.id = id
            self.image = image
            self.isPublic = isPublic
            self.localCreatedAt = localCreatedAt
            self.localUpdatedAt = localUpdatedAt
            self.name = name
            self.tags = tags
        }
    }

    // MARK: PacksCreatePackBody convenience initializers and mutators

    public extension PacksCreatePackBody {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksCreatePackBody.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            category: String?? = nil,
            description: String?? = nil,
            id: String? = nil,
            image: String?? = nil,
            isPublic: Bool?? = nil,
            localCreatedAt: Date? = nil,
            localUpdatedAt: Date? = nil,
            name: String? = nil,
            tags: [String]?? = nil
        ) -> PacksCreatePackBody {
            return PacksCreatePackBody(
                category: category ?? self.category,
                description: description ?? self.description,
                id: id ?? self.id,
                image: image ?? self.image,
                isPublic: isPublic ?? self.isPublic,
                localCreatedAt: localCreatedAt ?? self.localCreatedAt,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                name: name ?? self.name,
                tags: tags ?? self.tags
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksCreatePackWeightHistoryBody
    public struct PacksCreatePackWeightHistoryBody: Codable, Equatable {
        public let id: String
        public let localCreatedAt: Date
        public let weight: Double

        public init(id: String, localCreatedAt: Date, weight: Double) {
            self.id = id
            self.localCreatedAt = localCreatedAt
            self.weight = weight
        }
    }

    // MARK: PacksCreatePackWeightHistoryBody convenience initializers and mutators

    public extension PacksCreatePackWeightHistoryBody {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksCreatePackWeightHistoryBody.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            id: String? = nil,
            localCreatedAt: Date? = nil,
            weight: Double? = nil
        ) -> PacksCreatePackWeightHistoryBody {
            return PacksCreatePackWeightHistoryBody(
                id: id ?? self.id,
                localCreatedAt: localCreatedAt ?? self.localCreatedAt,
                weight: weight ?? self.weight
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksErrorResponse
    public struct PacksErrorResponse: Codable, Equatable {
        public let code: String?
        public let error: String

        public init(code: String?, error: String) {
            self.code = code
            self.error = error
        }
    }

    // MARK: PacksErrorResponse convenience initializers and mutators

    public extension PacksErrorResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksErrorResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            code: String?? = nil,
            error: String? = nil
        ) -> PacksErrorResponse {
            return PacksErrorResponse(
                code: code ?? self.code,
                error: error ?? self.error
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksGapAnalysisRequest
    public struct PacksGapAnalysisRequest: Codable, Equatable {
        public let destination: String?
        public let duration: Int?
        public let endDate, startDate, tripType: String?

        public init(destination: String?, duration: Int?, endDate: String?, startDate: String?, tripType: String?) {
            self.destination = destination
            self.duration = duration
            self.endDate = endDate
            self.startDate = startDate
            self.tripType = tripType
        }
    }

    // MARK: PacksGapAnalysisRequest convenience initializers and mutators

    public extension PacksGapAnalysisRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksGapAnalysisRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            destination: String?? = nil,
            duration: Int?? = nil,
            endDate: String?? = nil,
            startDate: String?? = nil,
            tripType: String?? = nil
        ) -> PacksGapAnalysisRequest {
            return PacksGapAnalysisRequest(
                destination: destination ?? self.destination,
                duration: duration ?? self.duration,
                endDate: endDate ?? self.endDate,
                startDate: startDate ?? self.startDate,
                tripType: tripType ?? self.tripType
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksPackItem
    public struct PacksPackItem: Codable, Equatable {
        public let catalogItemID: Int
        public let category: String
        public let consumable: Bool
        public let createdAt: Date
        public let deleted: Bool
        public let description, id, image: String
        public let isAIGenerated: Bool
        public let name, notes, packID: String
        public let quantity: Int
        public let templateItemID: String
        public let updatedAt: Date
        public let userID: String
        public let weight: Double
        public let weightUnit: WeightUnit
        public let worn: Bool

        public enum CodingKeys: String, CodingKey {
            case catalogItemID = "catalogItemId"
            case category, consumable, createdAt, deleted, description, id, image, isAIGenerated, name, notes
            case packID = "packId"
            case quantity
            case templateItemID = "templateItemId"
            case updatedAt
            case userID = "userId"
            case weight, weightUnit, worn
        }

        public init(catalogItemID: Int, category: String, consumable: Bool, createdAt: Date, deleted: Bool, description: String, id: String, image: String, isAIGenerated: Bool, name: String, notes: String, packID: String, quantity: Int, templateItemID: String, updatedAt: Date, userID: String, weight: Double, weightUnit: WeightUnit, worn: Bool) {
            self.catalogItemID = catalogItemID
            self.category = category
            self.consumable = consumable
            self.createdAt = createdAt
            self.deleted = deleted
            self.description = description
            self.id = id
            self.image = image
            self.isAIGenerated = isAIGenerated
            self.name = name
            self.notes = notes
            self.packID = packID
            self.quantity = quantity
            self.templateItemID = templateItemID
            self.updatedAt = updatedAt
            self.userID = userID
            self.weight = weight
            self.weightUnit = weightUnit
            self.worn = worn
        }
    }

    // MARK: PacksPackItem convenience initializers and mutators

    public extension PacksPackItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksPackItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            catalogItemID: Int? = nil,
            category: String? = nil,
            consumable: Bool? = nil,
            createdAt: Date? = nil,
            deleted: Bool? = nil,
            description: String? = nil,
            id: String? = nil,
            image: String? = nil,
            isAIGenerated: Bool? = nil,
            name: String? = nil,
            notes: String? = nil,
            packID: String? = nil,
            quantity: Int? = nil,
            templateItemID: String? = nil,
            updatedAt: Date? = nil,
            userID: String? = nil,
            weight: Double? = nil,
            weightUnit: WeightUnit? = nil,
            worn: Bool? = nil
        ) -> PacksPackItem {
            return PacksPackItem(
                catalogItemID: catalogItemID ?? self.catalogItemID,
                category: category ?? self.category,
                consumable: consumable ?? self.consumable,
                createdAt: createdAt ?? self.createdAt,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                id: id ?? self.id,
                image: image ?? self.image,
                isAIGenerated: isAIGenerated ?? self.isAIGenerated,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                packID: packID ?? self.packID,
                quantity: quantity ?? self.quantity,
                templateItemID: templateItemID ?? self.templateItemID,
                updatedAt: updatedAt ?? self.updatedAt,
                userID: userID ?? self.userID,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit,
                worn: worn ?? self.worn
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksPackWithWeights
    public struct PacksPackWithWeights: Codable, Equatable {
        public let baseWeight: Double
        public let category: Category
        public let createdAt: Date
        public let deleted: Bool
        public let description, id, image: String
        public let isAIGenerated, isPublic: Bool
        public let items: [PacksPackWithWeightsItem]?
        public let localCreatedAt, localUpdatedAt: Date?
        public let name: String
        public let tags: [String]
        public let templateID: String?
        public let totalWeight: Double
        public let updatedAt: Date
        public let userID: String

        public enum CodingKeys: String, CodingKey {
            case baseWeight, category, createdAt, deleted, description, id, image, isAIGenerated, isPublic, items, localCreatedAt, localUpdatedAt, name, tags
            case templateID = "templateId"
            case totalWeight, updatedAt
            case userID = "userId"
        }

        public init(baseWeight: Double, category: Category, createdAt: Date, deleted: Bool, description: String, id: String, image: String, isAIGenerated: Bool, isPublic: Bool, items: [PacksPackWithWeightsItem]?, localCreatedAt: Date?, localUpdatedAt: Date?, name: String, tags: [String], templateID: String?, totalWeight: Double, updatedAt: Date, userID: String) {
            self.baseWeight = baseWeight
            self.category = category
            self.createdAt = createdAt
            self.deleted = deleted
            self.description = description
            self.id = id
            self.image = image
            self.isAIGenerated = isAIGenerated
            self.isPublic = isPublic
            self.items = items
            self.localCreatedAt = localCreatedAt
            self.localUpdatedAt = localUpdatedAt
            self.name = name
            self.tags = tags
            self.templateID = templateID
            self.totalWeight = totalWeight
            self.updatedAt = updatedAt
            self.userID = userID
        }
    }

    // MARK: PacksPackWithWeights convenience initializers and mutators

    public extension PacksPackWithWeights {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksPackWithWeights.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            baseWeight: Double? = nil,
            category: Category? = nil,
            createdAt: Date? = nil,
            deleted: Bool? = nil,
            description: String? = nil,
            id: String? = nil,
            image: String? = nil,
            isAIGenerated: Bool? = nil,
            isPublic: Bool? = nil,
            items: [PacksPackWithWeightsItem]?? = nil,
            localCreatedAt: Date?? = nil,
            localUpdatedAt: Date?? = nil,
            name: String? = nil,
            tags: [String]? = nil,
            templateID: String?? = nil,
            totalWeight: Double? = nil,
            updatedAt: Date? = nil,
            userID: String? = nil
        ) -> PacksPackWithWeights {
            return PacksPackWithWeights(
                baseWeight: baseWeight ?? self.baseWeight,
                category: category ?? self.category,
                createdAt: createdAt ?? self.createdAt,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                id: id ?? self.id,
                image: image ?? self.image,
                isAIGenerated: isAIGenerated ?? self.isAIGenerated,
                isPublic: isPublic ?? self.isPublic,
                items: items ?? self.items,
                localCreatedAt: localCreatedAt ?? self.localCreatedAt,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                name: name ?? self.name,
                tags: tags ?? self.tags,
                templateID: templateID ?? self.templateID,
                totalWeight: totalWeight ?? self.totalWeight,
                updatedAt: updatedAt ?? self.updatedAt,
                userID: userID ?? self.userID
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksPackWithWeightsItem
    public struct PacksPackWithWeightsItem: Codable, Equatable {
        public let catalogItemID: Int
        public let category: String
        public let consumable: Bool
        public let createdAt: Date
        public let deleted: Bool
        public let description, id, image: String
        public let isAIGenerated: Bool
        public let name, notes, packID: String
        public let quantity: Int
        public let templateItemID: String
        public let updatedAt: Date
        public let userID: String
        public let weight: Double
        public let weightUnit: WeightUnit
        public let worn: Bool

        public enum CodingKeys: String, CodingKey {
            case catalogItemID = "catalogItemId"
            case category, consumable, createdAt, deleted, description, id, image, isAIGenerated, name, notes
            case packID = "packId"
            case quantity
            case templateItemID = "templateItemId"
            case updatedAt
            case userID = "userId"
            case weight, weightUnit, worn
        }

        public init(catalogItemID: Int, category: String, consumable: Bool, createdAt: Date, deleted: Bool, description: String, id: String, image: String, isAIGenerated: Bool, name: String, notes: String, packID: String, quantity: Int, templateItemID: String, updatedAt: Date, userID: String, weight: Double, weightUnit: WeightUnit, worn: Bool) {
            self.catalogItemID = catalogItemID
            self.category = category
            self.consumable = consumable
            self.createdAt = createdAt
            self.deleted = deleted
            self.description = description
            self.id = id
            self.image = image
            self.isAIGenerated = isAIGenerated
            self.name = name
            self.notes = notes
            self.packID = packID
            self.quantity = quantity
            self.templateItemID = templateItemID
            self.updatedAt = updatedAt
            self.userID = userID
            self.weight = weight
            self.weightUnit = weightUnit
            self.worn = worn
        }
    }

    // MARK: PacksPackWithWeightsItem convenience initializers and mutators

    public extension PacksPackWithWeightsItem {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksPackWithWeightsItem.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            catalogItemID: Int? = nil,
            category: String? = nil,
            consumable: Bool? = nil,
            createdAt: Date? = nil,
            deleted: Bool? = nil,
            description: String? = nil,
            id: String? = nil,
            image: String? = nil,
            isAIGenerated: Bool? = nil,
            name: String? = nil,
            notes: String? = nil,
            packID: String? = nil,
            quantity: Int? = nil,
            templateItemID: String? = nil,
            updatedAt: Date? = nil,
            userID: String? = nil,
            weight: Double? = nil,
            weightUnit: WeightUnit? = nil,
            worn: Bool? = nil
        ) -> PacksPackWithWeightsItem {
            return PacksPackWithWeightsItem(
                catalogItemID: catalogItemID ?? self.catalogItemID,
                category: category ?? self.category,
                consumable: consumable ?? self.consumable,
                createdAt: createdAt ?? self.createdAt,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                id: id ?? self.id,
                image: image ?? self.image,
                isAIGenerated: isAIGenerated ?? self.isAIGenerated,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                packID: packID ?? self.packID,
                quantity: quantity ?? self.quantity,
                templateItemID: templateItemID ?? self.templateItemID,
                updatedAt: updatedAt ?? self.updatedAt,
                userID: userID ?? self.userID,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit,
                worn: worn ?? self.worn
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksUpdatePackItemRequest
    public struct PacksUpdatePackItemRequest: Codable, Equatable {
        public let catalogItemID: Int?
        public let category: String?
        public let consumable, deleted: Bool?
        public let description, image: String?
        public let name: String?
        public let notes: String?
        public let quantity: Int?
        public let weight: Double?
        public let weightUnit: WeightUnit?
        public let worn: Bool?

        public enum CodingKeys: String, CodingKey {
            case catalogItemID = "catalogItemId"
            case category, consumable, deleted, description, image, name, notes, quantity, weight, weightUnit, worn
        }

        public init(catalogItemID: Int?, category: String?, consumable: Bool?, deleted: Bool?, description: String?, image: String?, name: String?, notes: String?, quantity: Int?, weight: Double?, weightUnit: WeightUnit?, worn: Bool?) {
            self.catalogItemID = catalogItemID
            self.category = category
            self.consumable = consumable
            self.deleted = deleted
            self.description = description
            self.image = image
            self.name = name
            self.notes = notes
            self.quantity = quantity
            self.weight = weight
            self.weightUnit = weightUnit
            self.worn = worn
        }
    }

    // MARK: PacksUpdatePackItemRequest convenience initializers and mutators

    public extension PacksUpdatePackItemRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksUpdatePackItemRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            catalogItemID: Int?? = nil,
            category: String?? = nil,
            consumable: Bool?? = nil,
            deleted: Bool?? = nil,
            description: String?? = nil,
            image: String?? = nil,
            name: String?? = nil,
            notes: String?? = nil,
            quantity: Int?? = nil,
            weight: Double?? = nil,
            weightUnit: WeightUnit?? = nil,
            worn: Bool?? = nil
        ) -> PacksUpdatePackItemRequest {
            return PacksUpdatePackItemRequest(
                catalogItemID: catalogItemID ?? self.catalogItemID,
                category: category ?? self.category,
                consumable: consumable ?? self.consumable,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                image: image ?? self.image,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                quantity: quantity ?? self.quantity,
                weight: weight ?? self.weight,
                weightUnit: weightUnit ?? self.weightUnit,
                worn: worn ?? self.worn
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PacksUpdatePackRequest
    public struct PacksUpdatePackRequest: Codable, Equatable {
        public let category: String?
        public let deleted: Bool?
        public let description, image: String?
        public let isPublic: Bool?
        public let name: String?
        public let tags: [String]?

        public init(category: String?, deleted: Bool?, description: String?, image: String?, isPublic: Bool?, name: String?, tags: [String]?) {
            self.category = category
            self.deleted = deleted
            self.description = description
            self.image = image
            self.isPublic = isPublic
            self.name = name
            self.tags = tags
        }
    }

    // MARK: PacksUpdatePackRequest convenience initializers and mutators

    public extension PacksUpdatePackRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PacksUpdatePackRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            category: String?? = nil,
            deleted: Bool?? = nil,
            description: String?? = nil,
            image: String?? = nil,
            isPublic: Bool?? = nil,
            name: String?? = nil,
            tags: [String]?? = nil
        ) -> PacksUpdatePackRequest {
            return PacksUpdatePackRequest(
                category: category ?? self.category,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                image: image ?? self.image,
                isPublic: isPublic ?? self.isPublic,
                name: name ?? self.name,
                tags: tags ?? self.tags
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PasswordResetForgotPasswordRequest
    public struct PasswordResetForgotPasswordRequest: Codable, Equatable {
        public let email: String

        public init(email: String) {
            self.email = email
        }
    }

    // MARK: PasswordResetForgotPasswordRequest convenience initializers and mutators

    public extension PasswordResetForgotPasswordRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PasswordResetForgotPasswordRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            email: String? = nil
        ) -> PasswordResetForgotPasswordRequest {
            return PasswordResetForgotPasswordRequest(
                email: email ?? self.email
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - PasswordResetResetPasswordRequest
    public struct PasswordResetResetPasswordRequest: Codable, Equatable {
        public let code: String
        public let email: String
        public let newPassword: String

        public init(code: String, email: String, newPassword: String) {
            self.code = code
            self.email = email
            self.newPassword = newPassword
        }
    }

    // MARK: PasswordResetResetPasswordRequest convenience initializers and mutators

    public extension PasswordResetResetPasswordRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(PasswordResetResetPasswordRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            code: String? = nil,
            email: String? = nil,
            newPassword: String? = nil
        ) -> PasswordResetResetPasswordRequest {
            return PasswordResetResetPasswordRequest(
                code: code ?? self.code,
                email: email ?? self.email,
                newPassword: newPassword ?? self.newPassword
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - SeasonSuggestionsSeasonSuggestionsRequest
    public struct SeasonSuggestionsSeasonSuggestionsRequest: Codable, Equatable {
        public let date, location: String

        public init(date: String, location: String) {
            self.date = date
            self.location = location
        }
    }

    // MARK: SeasonSuggestionsSeasonSuggestionsRequest convenience initializers and mutators

    public extension SeasonSuggestionsSeasonSuggestionsRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(SeasonSuggestionsSeasonSuggestionsRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            date: String? = nil,
            location: String? = nil
        ) -> SeasonSuggestionsSeasonSuggestionsRequest {
            return SeasonSuggestionsSeasonSuggestionsRequest(
                date: date ?? self.date,
                location: location ?? self.location
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TrailConditionsCreateTrailConditionReportRequest
    public struct TrailConditionsCreateTrailConditionReportRequest: Codable, Equatable {
        public let hazards: [String]?
        /// Client-generated report ID
        public let id: String
        public let localCreatedAt, localUpdatedAt: Date
        public let notes: String?
        public let overallCondition: OverallCondition
        public let photos: [String]?
        public let surface: Surface
        public let trailName: String
        public let trailRegion, tripID: String?
        public let waterCrossingDifficulty: WaterCrossingDifficulty?
        public let waterCrossings: Int?

        public enum CodingKeys: String, CodingKey {
            case hazards, id, localCreatedAt, localUpdatedAt, notes, overallCondition, photos, surface, trailName, trailRegion
            case tripID = "tripId"
            case waterCrossingDifficulty, waterCrossings
        }

        public init(hazards: [String]?, id: String, localCreatedAt: Date, localUpdatedAt: Date, notes: String?, overallCondition: OverallCondition, photos: [String]?, surface: Surface, trailName: String, trailRegion: String?, tripID: String?, waterCrossingDifficulty: WaterCrossingDifficulty?, waterCrossings: Int?) {
            self.hazards = hazards
            self.id = id
            self.localCreatedAt = localCreatedAt
            self.localUpdatedAt = localUpdatedAt
            self.notes = notes
            self.overallCondition = overallCondition
            self.photos = photos
            self.surface = surface
            self.trailName = trailName
            self.trailRegion = trailRegion
            self.tripID = tripID
            self.waterCrossingDifficulty = waterCrossingDifficulty
            self.waterCrossings = waterCrossings
        }
    }

    // MARK: TrailConditionsCreateTrailConditionReportRequest convenience initializers and mutators

    public extension TrailConditionsCreateTrailConditionReportRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TrailConditionsCreateTrailConditionReportRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            hazards: [String]?? = nil,
            id: String? = nil,
            localCreatedAt: Date? = nil,
            localUpdatedAt: Date? = nil,
            notes: String?? = nil,
            overallCondition: OverallCondition? = nil,
            photos: [String]?? = nil,
            surface: Surface? = nil,
            trailName: String? = nil,
            trailRegion: String?? = nil,
            tripID: String?? = nil,
            waterCrossingDifficulty: WaterCrossingDifficulty?? = nil,
            waterCrossings: Int?? = nil
        ) -> TrailConditionsCreateTrailConditionReportRequest {
            return TrailConditionsCreateTrailConditionReportRequest(
                hazards: hazards ?? self.hazards,
                id: id ?? self.id,
                localCreatedAt: localCreatedAt ?? self.localCreatedAt,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                notes: notes ?? self.notes,
                overallCondition: overallCondition ?? self.overallCondition,
                photos: photos ?? self.photos,
                surface: surface ?? self.surface,
                trailName: trailName ?? self.trailName,
                trailRegion: trailRegion ?? self.trailRegion,
                tripID: tripID ?? self.tripID,
                waterCrossingDifficulty: waterCrossingDifficulty ?? self.waterCrossingDifficulty,
                waterCrossings: waterCrossings ?? self.waterCrossings
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    public enum OverallCondition: String, Codable, Equatable {
        case excellent = "excellent"
        case fair = "fair"
        case good = "good"
        case poor = "poor"
    }

    public enum Surface: String, Codable, Equatable {
        case dirt = "dirt"
        case gravel = "gravel"
        case mud = "mud"
        case paved = "paved"
        case rocky = "rocky"
        case snow = "snow"
    }

    public enum WaterCrossingDifficulty: String, Codable, Equatable {
        case difficult = "difficult"
        case easy = "easy"
        case moderate = "moderate"
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TrailConditionsUpdateTrailConditionReportRequest
    public struct TrailConditionsUpdateTrailConditionReportRequest: Codable, Equatable {
        public let hazards: [String]?
        public let localUpdatedAt: Date?
        public let notes: String?
        public let overallCondition: OverallCondition?
        public let photos: [String]?
        public let surface: Surface?
        public let trailName: String?
        public let trailRegion, tripID: String?
        public let waterCrossingDifficulty: WaterCrossingDifficulty?
        public let waterCrossings: Int?

        public enum CodingKeys: String, CodingKey {
            case hazards, localUpdatedAt, notes, overallCondition, photos, surface, trailName, trailRegion
            case tripID = "tripId"
            case waterCrossingDifficulty, waterCrossings
        }

        public init(hazards: [String]?, localUpdatedAt: Date?, notes: String?, overallCondition: OverallCondition?, photos: [String]?, surface: Surface?, trailName: String?, trailRegion: String?, tripID: String?, waterCrossingDifficulty: WaterCrossingDifficulty?, waterCrossings: Int?) {
            self.hazards = hazards
            self.localUpdatedAt = localUpdatedAt
            self.notes = notes
            self.overallCondition = overallCondition
            self.photos = photos
            self.surface = surface
            self.trailName = trailName
            self.trailRegion = trailRegion
            self.tripID = tripID
            self.waterCrossingDifficulty = waterCrossingDifficulty
            self.waterCrossings = waterCrossings
        }
    }

    // MARK: TrailConditionsUpdateTrailConditionReportRequest convenience initializers and mutators

    public extension TrailConditionsUpdateTrailConditionReportRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TrailConditionsUpdateTrailConditionReportRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            hazards: [String]?? = nil,
            localUpdatedAt: Date?? = nil,
            notes: String?? = nil,
            overallCondition: OverallCondition?? = nil,
            photos: [String]?? = nil,
            surface: Surface?? = nil,
            trailName: String?? = nil,
            trailRegion: String?? = nil,
            tripID: String?? = nil,
            waterCrossingDifficulty: WaterCrossingDifficulty?? = nil,
            waterCrossings: Int?? = nil
        ) -> TrailConditionsUpdateTrailConditionReportRequest {
            return TrailConditionsUpdateTrailConditionReportRequest(
                hazards: hazards ?? self.hazards,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                notes: notes ?? self.notes,
                overallCondition: overallCondition ?? self.overallCondition,
                photos: photos ?? self.photos,
                surface: surface ?? self.surface,
                trailName: trailName ?? self.trailName,
                trailRegion: trailRegion ?? self.trailRegion,
                tripID: tripID ?? self.tripID,
                waterCrossingDifficulty: waterCrossingDifficulty ?? self.waterCrossingDifficulty,
                waterCrossings: waterCrossings ?? self.waterCrossings
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TrailsRouteDetailRow
    public struct TrailsRouteDetailRow: Codable, Equatable {
        public let description, difficulty, distance, geojson: String
        public let members: [Member]
        public let name, network, osmID, sport: String

        public enum CodingKeys: String, CodingKey {
            case description, difficulty, distance, geojson, members, name, network
            case osmID = "osm_id"
            case sport
        }

        public init(description: String, difficulty: String, distance: String, geojson: String, members: [Member], name: String, network: String, osmID: String, sport: String) {
            self.description = description
            self.difficulty = difficulty
            self.distance = distance
            self.geojson = geojson
            self.members = members
            self.name = name
            self.network = network
            self.osmID = osmID
            self.sport = sport
        }
    }

    // MARK: TrailsRouteDetailRow convenience initializers and mutators

    public extension TrailsRouteDetailRow {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TrailsRouteDetailRow.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            description: String? = nil,
            difficulty: String? = nil,
            distance: String? = nil,
            geojson: String? = nil,
            members: [Member]? = nil,
            name: String? = nil,
            network: String? = nil,
            osmID: String? = nil,
            sport: String? = nil
        ) -> TrailsRouteDetailRow {
            return TrailsRouteDetailRow(
                description: description ?? self.description,
                difficulty: difficulty ?? self.difficulty,
                distance: distance ?? self.distance,
                geojson: geojson ?? self.geojson,
                members: members ?? self.members,
                name: name ?? self.name,
                network: network ?? self.network,
                osmID: osmID ?? self.osmID,
                sport: sport ?? self.sport
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Member
    public struct Member: Codable, Equatable {
        public let ref: Int
        public let role, type: String

        public init(ref: Int, role: String, type: String) {
            self.ref = ref
            self.role = role
            self.type = type
        }
    }

    // MARK: Member convenience initializers and mutators

    public extension Member {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Member.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            ref: Int? = nil,
            role: String? = nil,
            type: String? = nil
        ) -> Member {
            return Member(
                ref: ref ?? self.ref,
                role: role ?? self.role,
                type: type ?? self.type
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TrailsRouteSearchRow
    public struct TrailsRouteSearchRow: Codable, Equatable {
        public let bbox, description, difficulty, distance: String
        public let name, network, osmID, sport: String

        public enum CodingKeys: String, CodingKey {
            case bbox, description, difficulty, distance, name, network
            case osmID = "osm_id"
            case sport
        }

        public init(bbox: String, description: String, difficulty: String, distance: String, name: String, network: String, osmID: String, sport: String) {
            self.bbox = bbox
            self.description = description
            self.difficulty = difficulty
            self.distance = distance
            self.name = name
            self.network = network
            self.osmID = osmID
            self.sport = sport
        }
    }

    // MARK: TrailsRouteSearchRow convenience initializers and mutators

    public extension TrailsRouteSearchRow {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TrailsRouteSearchRow.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            bbox: String? = nil,
            description: String? = nil,
            difficulty: String? = nil,
            distance: String? = nil,
            name: String? = nil,
            network: String? = nil,
            osmID: String? = nil,
            sport: String? = nil
        ) -> TrailsRouteSearchRow {
            return TrailsRouteSearchRow(
                bbox: bbox ?? self.bbox,
                description: description ?? self.description,
                difficulty: difficulty ?? self.difficulty,
                distance: distance ?? self.distance,
                name: name ?? self.name,
                network: network ?? self.network,
                osmID: osmID ?? self.osmID,
                sport: sport ?? self.sport
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TripsCreateTripBody
    public struct TripsCreateTripBody: Codable, Equatable {
        public let description, endDate: String?
        public let id: String
        public let localCreatedAt, localUpdatedAt: Date
        public let location: TripsCreateTripBodyLocation?
        public let name: String
        public let notes, packID, startDate: String?

        public enum CodingKeys: String, CodingKey {
            case description, endDate, id, localCreatedAt, localUpdatedAt, location, name, notes
            case packID = "packId"
            case startDate
        }

        public init(description: String?, endDate: String?, id: String, localCreatedAt: Date, localUpdatedAt: Date, location: TripsCreateTripBodyLocation?, name: String, notes: String?, packID: String?, startDate: String?) {
            self.description = description
            self.endDate = endDate
            self.id = id
            self.localCreatedAt = localCreatedAt
            self.localUpdatedAt = localUpdatedAt
            self.location = location
            self.name = name
            self.notes = notes
            self.packID = packID
            self.startDate = startDate
        }
    }

    // MARK: TripsCreateTripBody convenience initializers and mutators

    public extension TripsCreateTripBody {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TripsCreateTripBody.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            description: String?? = nil,
            endDate: String?? = nil,
            id: String? = nil,
            localCreatedAt: Date? = nil,
            localUpdatedAt: Date? = nil,
            location: TripsCreateTripBodyLocation?? = nil,
            name: String? = nil,
            notes: String?? = nil,
            packID: String?? = nil,
            startDate: String?? = nil
        ) -> TripsCreateTripBody {
            return TripsCreateTripBody(
                description: description ?? self.description,
                endDate: endDate ?? self.endDate,
                id: id ?? self.id,
                localCreatedAt: localCreatedAt ?? self.localCreatedAt,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                location: location ?? self.location,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                packID: packID ?? self.packID,
                startDate: startDate ?? self.startDate
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TripsCreateTripBodyLocation
    public struct TripsCreateTripBodyLocation: Codable, Equatable {
        public let latitude, longitude: Double
        public let name: String?

        public init(latitude: Double, longitude: Double, name: String?) {
            self.latitude = latitude
            self.longitude = longitude
            self.name = name
        }
    }

    // MARK: TripsCreateTripBodyLocation convenience initializers and mutators

    public extension TripsCreateTripBodyLocation {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TripsCreateTripBodyLocation.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            latitude: Double? = nil,
            longitude: Double? = nil,
            name: String?? = nil
        ) -> TripsCreateTripBodyLocation {
            return TripsCreateTripBodyLocation(
                latitude: latitude ?? self.latitude,
                longitude: longitude ?? self.longitude,
                name: name ?? self.name
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TripsTrip
    public struct TripsTrip: Codable, Equatable {
        public let createdAt: Date?
        public let deleted: Bool
        public let description, endDate: String?
        public let id: String
        public let localCreatedAt, localUpdatedAt: Date?
        public let location: TripsTripLocation?
        public let name: String
        public let notes, packID, startDate: String?
        public let updatedAt: Date?
        public let userID: String?

        public enum CodingKeys: String, CodingKey {
            case createdAt, deleted, description, endDate, id, localCreatedAt, localUpdatedAt, location, name, notes
            case packID = "packId"
            case startDate, updatedAt
            case userID = "userId"
        }

        public init(createdAt: Date?, deleted: Bool, description: String?, endDate: String?, id: String, localCreatedAt: Date?, localUpdatedAt: Date?, location: TripsTripLocation?, name: String, notes: String?, packID: String?, startDate: String?, updatedAt: Date?, userID: String?) {
            self.createdAt = createdAt
            self.deleted = deleted
            self.description = description
            self.endDate = endDate
            self.id = id
            self.localCreatedAt = localCreatedAt
            self.localUpdatedAt = localUpdatedAt
            self.location = location
            self.name = name
            self.notes = notes
            self.packID = packID
            self.startDate = startDate
            self.updatedAt = updatedAt
            self.userID = userID
        }
    }

    // MARK: TripsTrip convenience initializers and mutators

    public extension TripsTrip {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TripsTrip.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            createdAt: Date?? = nil,
            deleted: Bool? = nil,
            description: String?? = nil,
            endDate: String?? = nil,
            id: String? = nil,
            localCreatedAt: Date?? = nil,
            localUpdatedAt: Date?? = nil,
            location: TripsTripLocation?? = nil,
            name: String? = nil,
            notes: String?? = nil,
            packID: String?? = nil,
            startDate: String?? = nil,
            updatedAt: Date?? = nil,
            userID: String?? = nil
        ) -> TripsTrip {
            return TripsTrip(
                createdAt: createdAt ?? self.createdAt,
                deleted: deleted ?? self.deleted,
                description: description ?? self.description,
                endDate: endDate ?? self.endDate,
                id: id ?? self.id,
                localCreatedAt: localCreatedAt ?? self.localCreatedAt,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                location: location ?? self.location,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                packID: packID ?? self.packID,
                startDate: startDate ?? self.startDate,
                updatedAt: updatedAt ?? self.updatedAt,
                userID: userID ?? self.userID
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TripsTripLocation
    public struct TripsTripLocation: Codable, Equatable {
        public let latitude, longitude: Double
        public let name: String?

        public init(latitude: Double, longitude: Double, name: String?) {
            self.latitude = latitude
            self.longitude = longitude
            self.name = name
        }
    }

    // MARK: TripsTripLocation convenience initializers and mutators

    public extension TripsTripLocation {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TripsTripLocation.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            latitude: Double? = nil,
            longitude: Double? = nil,
            name: String?? = nil
        ) -> TripsTripLocation {
            return TripsTripLocation(
                latitude: latitude ?? self.latitude,
                longitude: longitude ?? self.longitude,
                name: name ?? self.name
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TripsUpdateTripBody
    public struct TripsUpdateTripBody: Codable, Equatable {
        public let description, endDate: String?
        public let localUpdatedAt: Date?
        public let location: TripsUpdateTripBodyLocation?
        public let name: String?
        public let notes, packID, startDate: String?

        public enum CodingKeys: String, CodingKey {
            case description, endDate, localUpdatedAt, location, name, notes
            case packID = "packId"
            case startDate
        }

        public init(description: String?, endDate: String?, localUpdatedAt: Date?, location: TripsUpdateTripBodyLocation?, name: String?, notes: String?, packID: String?, startDate: String?) {
            self.description = description
            self.endDate = endDate
            self.localUpdatedAt = localUpdatedAt
            self.location = location
            self.name = name
            self.notes = notes
            self.packID = packID
            self.startDate = startDate
        }
    }

    // MARK: TripsUpdateTripBody convenience initializers and mutators

    public extension TripsUpdateTripBody {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TripsUpdateTripBody.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            description: String?? = nil,
            endDate: String?? = nil,
            localUpdatedAt: Date?? = nil,
            location: TripsUpdateTripBodyLocation?? = nil,
            name: String?? = nil,
            notes: String?? = nil,
            packID: String?? = nil,
            startDate: String?? = nil
        ) -> TripsUpdateTripBody {
            return TripsUpdateTripBody(
                description: description ?? self.description,
                endDate: endDate ?? self.endDate,
                localUpdatedAt: localUpdatedAt ?? self.localUpdatedAt,
                location: location ?? self.location,
                name: name ?? self.name,
                notes: notes ?? self.notes,
                packID: packID ?? self.packID,
                startDate: startDate ?? self.startDate
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - TripsUpdateTripBodyLocation
    public struct TripsUpdateTripBodyLocation: Codable, Equatable {
        public let latitude, longitude: Double
        public let name: String?

        public init(latitude: Double, longitude: Double, name: String?) {
            self.latitude = latitude
            self.longitude = longitude
            self.name = name
        }
    }

    // MARK: TripsUpdateTripBodyLocation convenience initializers and mutators

    public extension TripsUpdateTripBodyLocation {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(TripsUpdateTripBodyLocation.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            latitude: Double? = nil,
            longitude: Double? = nil,
            name: String?? = nil
        ) -> TripsUpdateTripBodyLocation {
            return TripsUpdateTripBodyLocation(
                latitude: latitude ?? self.latitude,
                longitude: longitude ?? self.longitude,
                name: name ?? self.name
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - UploadPresignedUploadResponse
    public struct UploadPresignedUploadResponse: Codable, Equatable {
        public let objectKey, publicURL, url: String

        public enum CodingKeys: String, CodingKey {
            case objectKey
            case publicURL = "publicUrl"
            case url
        }

        public init(objectKey: String, publicURL: String, url: String) {
            self.objectKey = objectKey
            self.publicURL = publicURL
            self.url = url
        }
    }

    // MARK: UploadPresignedUploadResponse convenience initializers and mutators

    public extension UploadPresignedUploadResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(UploadPresignedUploadResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            objectKey: String? = nil,
            publicURL: String? = nil,
            url: String? = nil
        ) -> UploadPresignedUploadResponse {
            return UploadPresignedUploadResponse(
                objectKey: objectKey ?? self.objectKey,
                publicURL: publicURL ?? self.publicURL,
                url: url ?? self.url
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - UserErrorResponse
    public struct UserErrorResponse: Codable, Equatable {
        public let code: String?
        public let error: String

        public init(code: String?, error: String) {
            self.code = code
            self.error = error
        }
    }

    // MARK: UserErrorResponse convenience initializers and mutators

    public extension UserErrorResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(UserErrorResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            code: String?? = nil,
            error: String? = nil
        ) -> UserErrorResponse {
            return UserErrorResponse(
                code: code ?? self.code,
                error: error ?? self.error
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - UserUpdateUserRequest
    public struct UserUpdateUserRequest: Codable, Equatable {
        public let avatarURL, email, firstName, lastName: String?

        public enum CodingKeys: String, CodingKey {
            case avatarURL = "avatarUrl"
            case email, firstName, lastName
        }

        public init(avatarURL: String?, email: String?, firstName: String?, lastName: String?) {
            self.avatarURL = avatarURL
            self.email = email
            self.firstName = firstName
            self.lastName = lastName
        }
    }

    // MARK: UserUpdateUserRequest convenience initializers and mutators

    public extension UserUpdateUserRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(UserUpdateUserRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            avatarURL: String?? = nil,
            email: String?? = nil,
            firstName: String?? = nil,
            lastName: String?? = nil
        ) -> UserUpdateUserRequest {
            return UserUpdateUserRequest(
                avatarURL: avatarURL ?? self.avatarURL,
                email: email ?? self.email,
                firstName: firstName ?? self.firstName,
                lastName: lastName ?? self.lastName
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - UserUpdateUserResponse
    public struct UserUpdateUserResponse: Codable, Equatable {
        public let message: String
        public let success: Bool
        public let user: UserUpdateUserResponseUser

        public init(message: String, success: Bool, user: UserUpdateUserResponseUser) {
            self.message = message
            self.success = success
            self.user = user
        }
    }

    // MARK: UserUpdateUserResponse convenience initializers and mutators

    public extension UserUpdateUserResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(UserUpdateUserResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            message: String? = nil,
            success: Bool? = nil,
            user: UserUpdateUserResponseUser? = nil
        ) -> UserUpdateUserResponse {
            return UserUpdateUserResponse(
                message: message ?? self.message,
                success: success ?? self.success,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - UserUpdateUserResponseUser
    public struct UserUpdateUserResponseUser: Codable, Equatable {
        public let avatarURL: String?
        public let createdAt, email: String
        public let emailVerified: Bool
        public let firstName, id, lastName: String
        public let role: String?
        public let updatedAt: String

        public enum CodingKeys: String, CodingKey {
            case avatarURL = "avatarUrl"
            case createdAt, email, emailVerified, firstName, id, lastName, role, updatedAt
        }

        public init(avatarURL: String?, createdAt: String, email: String, emailVerified: Bool, firstName: String, id: String, lastName: String, role: String?, updatedAt: String) {
            self.avatarURL = avatarURL
            self.createdAt = createdAt
            self.email = email
            self.emailVerified = emailVerified
            self.firstName = firstName
            self.id = id
            self.lastName = lastName
            self.role = role
            self.updatedAt = updatedAt
        }
    }

    // MARK: UserUpdateUserResponseUser convenience initializers and mutators

    public extension UserUpdateUserResponseUser {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(UserUpdateUserResponseUser.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            avatarURL: String?? = nil,
            createdAt: String? = nil,
            email: String? = nil,
            emailVerified: Bool? = nil,
            firstName: String? = nil,
            id: String? = nil,
            lastName: String? = nil,
            role: String?? = nil,
            updatedAt: String? = nil
        ) -> UserUpdateUserResponseUser {
            return UserUpdateUserResponseUser(
                avatarURL: avatarURL ?? self.avatarURL,
                createdAt: createdAt ?? self.createdAt,
                email: email ?? self.email,
                emailVerified: emailVerified ?? self.emailVerified,
                firstName: firstName ?? self.firstName,
                id: id ?? self.id,
                lastName: lastName ?? self.lastName,
                role: role ?? self.role,
                updatedAt: updatedAt ?? self.updatedAt
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - UserUserProfile
    public struct UserUserProfile: Codable, Equatable {
        public let success: Bool
        public let user: UserUserProfileUser

        public init(success: Bool, user: UserUserProfileUser) {
            self.success = success
            self.user = user
        }
    }

    // MARK: UserUserProfile convenience initializers and mutators

    public extension UserUserProfile {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(UserUserProfile.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            success: Bool? = nil,
            user: UserUserProfileUser? = nil
        ) -> UserUserProfile {
            return UserUserProfile(
                success: success ?? self.success,
                user: user ?? self.user
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - UserUserProfileUser
    public struct UserUserProfileUser: Codable, Equatable {
        public let avatarURL: String?
        public let createdAt, email: String
        public let emailVerified: Bool
        public let firstName, id, lastName: String
        public let role: String?
        public let updatedAt: String

        public enum CodingKeys: String, CodingKey {
            case avatarURL = "avatarUrl"
            case createdAt, email, emailVerified, firstName, id, lastName, role, updatedAt
        }

        public init(avatarURL: String?, createdAt: String, email: String, emailVerified: Bool, firstName: String, id: String, lastName: String, role: String?, updatedAt: String) {
            self.avatarURL = avatarURL
            self.createdAt = createdAt
            self.email = email
            self.emailVerified = emailVerified
            self.firstName = firstName
            self.id = id
            self.lastName = lastName
            self.role = role
            self.updatedAt = updatedAt
        }
    }

    // MARK: UserUserProfileUser convenience initializers and mutators

    public extension UserUserProfileUser {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(UserUserProfileUser.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            avatarURL: String?? = nil,
            createdAt: String? = nil,
            email: String? = nil,
            emailVerified: Bool? = nil,
            firstName: String? = nil,
            id: String? = nil,
            lastName: String? = nil,
            role: String?? = nil,
            updatedAt: String? = nil
        ) -> UserUserProfileUser {
            return UserUserProfileUser(
                avatarURL: avatarURL ?? self.avatarURL,
                createdAt: createdAt ?? self.createdAt,
                email: email ?? self.email,
                emailVerified: emailVerified ?? self.emailVerified,
                firstName: firstName ?? self.firstName,
                id: id ?? self.id,
                lastName: lastName ?? self.lastName,
                role: role ?? self.role,
                updatedAt: updatedAt ?? self.updatedAt
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - WeatherForecastResponse
    public struct WeatherForecastResponse: Codable, Equatable {
        public let alerts: Alerts?
        public let current: Current
        public let forecast: Forecast
        public let location: WeatherForecastResponseLocation

        public init(alerts: Alerts?, current: Current, forecast: Forecast, location: WeatherForecastResponseLocation) {
            self.alerts = alerts
            self.current = current
            self.forecast = forecast
            self.location = location
        }
    }

    // MARK: WeatherForecastResponse convenience initializers and mutators

    public extension WeatherForecastResponse {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(WeatherForecastResponse.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            alerts: Alerts?? = nil,
            current: Current? = nil,
            forecast: Forecast? = nil,
            location: WeatherForecastResponseLocation? = nil
        ) -> WeatherForecastResponse {
            return WeatherForecastResponse(
                alerts: alerts ?? self.alerts,
                current: current ?? self.current,
                forecast: forecast ?? self.forecast,
                location: location ?? self.location
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Alerts
    public struct Alerts: Codable, Equatable {
        public let alert: [Alert]?

        public init(alert: [Alert]?) {
            self.alert = alert
        }
    }

    // MARK: Alerts convenience initializers and mutators

    public extension Alerts {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Alerts.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            alert: [Alert]?? = nil
        ) -> Alerts {
            return Alerts(
                alert: alert ?? self.alert
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Alert
    public struct Alert: Codable, Equatable {
        public let areas, category, certainty, desc: String
        public let effective, event, expires, headline: String
        public let instruction: String?
        public let msgtype: String
        public let note: String?
        public let severity, urgency: String

        public init(areas: String, category: String, certainty: String, desc: String, effective: String, event: String, expires: String, headline: String, instruction: String?, msgtype: String, note: String?, severity: String, urgency: String) {
            self.areas = areas
            self.category = category
            self.certainty = certainty
            self.desc = desc
            self.effective = effective
            self.event = event
            self.expires = expires
            self.headline = headline
            self.instruction = instruction
            self.msgtype = msgtype
            self.note = note
            self.severity = severity
            self.urgency = urgency
        }
    }

    // MARK: Alert convenience initializers and mutators

    public extension Alert {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Alert.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            areas: String? = nil,
            category: String? = nil,
            certainty: String? = nil,
            desc: String? = nil,
            effective: String? = nil,
            event: String? = nil,
            expires: String? = nil,
            headline: String? = nil,
            instruction: String?? = nil,
            msgtype: String? = nil,
            note: String?? = nil,
            severity: String? = nil,
            urgency: String? = nil
        ) -> Alert {
            return Alert(
                areas: areas ?? self.areas,
                category: category ?? self.category,
                certainty: certainty ?? self.certainty,
                desc: desc ?? self.desc,
                effective: effective ?? self.effective,
                event: event ?? self.event,
                expires: expires ?? self.expires,
                headline: headline ?? self.headline,
                instruction: instruction ?? self.instruction,
                msgtype: msgtype ?? self.msgtype,
                note: note ?? self.note,
                severity: severity ?? self.severity,
                urgency: urgency ?? self.urgency
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Current
    public struct Current: Codable, Equatable {
        public let airQuality: CurrentAirQuality?
        public let chanceOfRain, chanceOfSnow: Double?
        public let cloud: Double
        public let condition: CurrentCondition
        public let dewpointC, dewpointF, diffRAD, dni: Double?
        public let feelslikeC, feelslikeF: Double
        public let gti, gustKph, gustMph, heatindexC: Double?
        public let heatindexF: Double?
        public let humidity, isDay: Double
        public let lastUpdated: String
        public let precipIn, precipMm, pressureIn, pressureMB: Double
        public let shortRAD, snowCM: Double?
        public let tempC, tempF, uv, visKM: Double
        public let visMiles: Double
        public let willItRain, willItSnow: Double?
        public let windDegree: Double
        public let windDir: String
        public let windKph, windMph: Double
        public let windchillC, windchillF: Double?

        public enum CodingKeys: String, CodingKey {
            case airQuality = "air_quality"
            case chanceOfRain = "chance_of_rain"
            case chanceOfSnow = "chance_of_snow"
            case cloud, condition
            case dewpointC = "dewpoint_c"
            case dewpointF = "dewpoint_f"
            case diffRAD = "diff_rad"
            case dni
            case feelslikeC = "feelslike_c"
            case feelslikeF = "feelslike_f"
            case gti
            case gustKph = "gust_kph"
            case gustMph = "gust_mph"
            case heatindexC = "heatindex_c"
            case heatindexF = "heatindex_f"
            case humidity
            case isDay = "is_day"
            case lastUpdated = "last_updated"
            case precipIn = "precip_in"
            case precipMm = "precip_mm"
            case pressureIn = "pressure_in"
            case pressureMB = "pressure_mb"
            case shortRAD = "short_rad"
            case snowCM = "snow_cm"
            case tempC = "temp_c"
            case tempF = "temp_f"
            case uv
            case visKM = "vis_km"
            case visMiles = "vis_miles"
            case willItRain = "will_it_rain"
            case willItSnow = "will_it_snow"
            case windDegree = "wind_degree"
            case windDir = "wind_dir"
            case windKph = "wind_kph"
            case windMph = "wind_mph"
            case windchillC = "windchill_c"
            case windchillF = "windchill_f"
        }

        public init(airQuality: CurrentAirQuality?, chanceOfRain: Double?, chanceOfSnow: Double?, cloud: Double, condition: CurrentCondition, dewpointC: Double?, dewpointF: Double?, diffRAD: Double?, dni: Double?, feelslikeC: Double, feelslikeF: Double, gti: Double?, gustKph: Double?, gustMph: Double?, heatindexC: Double?, heatindexF: Double?, humidity: Double, isDay: Double, lastUpdated: String, precipIn: Double, precipMm: Double, pressureIn: Double, pressureMB: Double, shortRAD: Double?, snowCM: Double?, tempC: Double, tempF: Double, uv: Double, visKM: Double, visMiles: Double, willItRain: Double?, willItSnow: Double?, windDegree: Double, windDir: String, windKph: Double, windMph: Double, windchillC: Double?, windchillF: Double?) {
            self.airQuality = airQuality
            self.chanceOfRain = chanceOfRain
            self.chanceOfSnow = chanceOfSnow
            self.cloud = cloud
            self.condition = condition
            self.dewpointC = dewpointC
            self.dewpointF = dewpointF
            self.diffRAD = diffRAD
            self.dni = dni
            self.feelslikeC = feelslikeC
            self.feelslikeF = feelslikeF
            self.gti = gti
            self.gustKph = gustKph
            self.gustMph = gustMph
            self.heatindexC = heatindexC
            self.heatindexF = heatindexF
            self.humidity = humidity
            self.isDay = isDay
            self.lastUpdated = lastUpdated
            self.precipIn = precipIn
            self.precipMm = precipMm
            self.pressureIn = pressureIn
            self.pressureMB = pressureMB
            self.shortRAD = shortRAD
            self.snowCM = snowCM
            self.tempC = tempC
            self.tempF = tempF
            self.uv = uv
            self.visKM = visKM
            self.visMiles = visMiles
            self.willItRain = willItRain
            self.willItSnow = willItSnow
            self.windDegree = windDegree
            self.windDir = windDir
            self.windKph = windKph
            self.windMph = windMph
            self.windchillC = windchillC
            self.windchillF = windchillF
        }
    }

    // MARK: Current convenience initializers and mutators

    public extension Current {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Current.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            airQuality: CurrentAirQuality?? = nil,
            chanceOfRain: Double?? = nil,
            chanceOfSnow: Double?? = nil,
            cloud: Double? = nil,
            condition: CurrentCondition? = nil,
            dewpointC: Double?? = nil,
            dewpointF: Double?? = nil,
            diffRAD: Double?? = nil,
            dni: Double?? = nil,
            feelslikeC: Double? = nil,
            feelslikeF: Double? = nil,
            gti: Double?? = nil,
            gustKph: Double?? = nil,
            gustMph: Double?? = nil,
            heatindexC: Double?? = nil,
            heatindexF: Double?? = nil,
            humidity: Double? = nil,
            isDay: Double? = nil,
            lastUpdated: String? = nil,
            precipIn: Double? = nil,
            precipMm: Double? = nil,
            pressureIn: Double? = nil,
            pressureMB: Double? = nil,
            shortRAD: Double?? = nil,
            snowCM: Double?? = nil,
            tempC: Double? = nil,
            tempF: Double? = nil,
            uv: Double? = nil,
            visKM: Double? = nil,
            visMiles: Double? = nil,
            willItRain: Double?? = nil,
            willItSnow: Double?? = nil,
            windDegree: Double? = nil,
            windDir: String? = nil,
            windKph: Double? = nil,
            windMph: Double? = nil,
            windchillC: Double?? = nil,
            windchillF: Double?? = nil
        ) -> Current {
            return Current(
                airQuality: airQuality ?? self.airQuality,
                chanceOfRain: chanceOfRain ?? self.chanceOfRain,
                chanceOfSnow: chanceOfSnow ?? self.chanceOfSnow,
                cloud: cloud ?? self.cloud,
                condition: condition ?? self.condition,
                dewpointC: dewpointC ?? self.dewpointC,
                dewpointF: dewpointF ?? self.dewpointF,
                diffRAD: diffRAD ?? self.diffRAD,
                dni: dni ?? self.dni,
                feelslikeC: feelslikeC ?? self.feelslikeC,
                feelslikeF: feelslikeF ?? self.feelslikeF,
                gti: gti ?? self.gti,
                gustKph: gustKph ?? self.gustKph,
                gustMph: gustMph ?? self.gustMph,
                heatindexC: heatindexC ?? self.heatindexC,
                heatindexF: heatindexF ?? self.heatindexF,
                humidity: humidity ?? self.humidity,
                isDay: isDay ?? self.isDay,
                lastUpdated: lastUpdated ?? self.lastUpdated,
                precipIn: precipIn ?? self.precipIn,
                precipMm: precipMm ?? self.precipMm,
                pressureIn: pressureIn ?? self.pressureIn,
                pressureMB: pressureMB ?? self.pressureMB,
                shortRAD: shortRAD ?? self.shortRAD,
                snowCM: snowCM ?? self.snowCM,
                tempC: tempC ?? self.tempC,
                tempF: tempF ?? self.tempF,
                uv: uv ?? self.uv,
                visKM: visKM ?? self.visKM,
                visMiles: visMiles ?? self.visMiles,
                willItRain: willItRain ?? self.willItRain,
                willItSnow: willItSnow ?? self.willItSnow,
                windDegree: windDegree ?? self.windDegree,
                windDir: windDir ?? self.windDir,
                windKph: windKph ?? self.windKph,
                windMph: windMph ?? self.windMph,
                windchillC: windchillC ?? self.windchillC,
                windchillF: windchillF ?? self.windchillF
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CurrentAirQuality
    public struct CurrentAirQuality: Codable, Equatable {
        public let co, gbDefraIndex, no2, o3: Double
        public let pm10, pm25, so2, usEpaIndex: Double

        public enum CodingKeys: String, CodingKey {
            case co
            case gbDefraIndex = "gb-defra-index"
            case no2, o3, pm10
            case pm25 = "pm2_5"
            case so2
            case usEpaIndex = "us-epa-index"
        }

        public init(co: Double, gbDefraIndex: Double, no2: Double, o3: Double, pm10: Double, pm25: Double, so2: Double, usEpaIndex: Double) {
            self.co = co
            self.gbDefraIndex = gbDefraIndex
            self.no2 = no2
            self.o3 = o3
            self.pm10 = pm10
            self.pm25 = pm25
            self.so2 = so2
            self.usEpaIndex = usEpaIndex
        }
    }

    // MARK: CurrentAirQuality convenience initializers and mutators

    public extension CurrentAirQuality {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CurrentAirQuality.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            co: Double? = nil,
            gbDefraIndex: Double? = nil,
            no2: Double? = nil,
            o3: Double? = nil,
            pm10: Double? = nil,
            pm25: Double? = nil,
            so2: Double? = nil,
            usEpaIndex: Double? = nil
        ) -> CurrentAirQuality {
            return CurrentAirQuality(
                co: co ?? self.co,
                gbDefraIndex: gbDefraIndex ?? self.gbDefraIndex,
                no2: no2 ?? self.no2,
                o3: o3 ?? self.o3,
                pm10: pm10 ?? self.pm10,
                pm25: pm25 ?? self.pm25,
                so2: so2 ?? self.so2,
                usEpaIndex: usEpaIndex ?? self.usEpaIndex
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - CurrentCondition
    public struct CurrentCondition: Codable, Equatable {
        public let code: Double
        public let icon, text: String

        public init(code: Double, icon: String, text: String) {
            self.code = code
            self.icon = icon
            self.text = text
        }
    }

    // MARK: CurrentCondition convenience initializers and mutators

    public extension CurrentCondition {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(CurrentCondition.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            code: Double? = nil,
            icon: String? = nil,
            text: String? = nil
        ) -> CurrentCondition {
            return CurrentCondition(
                code: code ?? self.code,
                icon: icon ?? self.icon,
                text: text ?? self.text
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Forecast
    public struct Forecast: Codable, Equatable {
        public let forecastday: [Forecastday]

        public init(forecastday: [Forecastday]) {
            self.forecastday = forecastday
        }
    }

    // MARK: Forecast convenience initializers and mutators

    public extension Forecast {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Forecast.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            forecastday: [Forecastday]? = nil
        ) -> Forecast {
            return Forecast(
                forecastday: forecastday ?? self.forecastday
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Forecastday
    public struct Forecastday: Codable, Equatable {
        public let astro: Astro?
        public let date: String
        public let dateEpoch: Double
        public let day: Day
        public let hour: [Hour]

        public enum CodingKeys: String, CodingKey {
            case astro, date
            case dateEpoch = "date_epoch"
            case day, hour
        }

        public init(astro: Astro?, date: String, dateEpoch: Double, day: Day, hour: [Hour]) {
            self.astro = astro
            self.date = date
            self.dateEpoch = dateEpoch
            self.day = day
            self.hour = hour
        }
    }

    // MARK: Forecastday convenience initializers and mutators

    public extension Forecastday {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Forecastday.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            astro: Astro?? = nil,
            date: String? = nil,
            dateEpoch: Double? = nil,
            day: Day? = nil,
            hour: [Hour]? = nil
        ) -> Forecastday {
            return Forecastday(
                astro: astro ?? self.astro,
                date: date ?? self.date,
                dateEpoch: dateEpoch ?? self.dateEpoch,
                day: day ?? self.day,
                hour: hour ?? self.hour
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Astro
    public struct Astro: Codable, Equatable {
        public let moonIllumination: Double
        public let moonPhase, moonrise, moonset, sunrise: String
        public let sunset: String

        public enum CodingKeys: String, CodingKey {
            case moonIllumination = "moon_illumination"
            case moonPhase = "moon_phase"
            case moonrise, moonset, sunrise, sunset
        }

        public init(moonIllumination: Double, moonPhase: String, moonrise: String, moonset: String, sunrise: String, sunset: String) {
            self.moonIllumination = moonIllumination
            self.moonPhase = moonPhase
            self.moonrise = moonrise
            self.moonset = moonset
            self.sunrise = sunrise
            self.sunset = sunset
        }
    }

    // MARK: Astro convenience initializers and mutators

    public extension Astro {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Astro.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            moonIllumination: Double? = nil,
            moonPhase: String? = nil,
            moonrise: String? = nil,
            moonset: String? = nil,
            sunrise: String? = nil,
            sunset: String? = nil
        ) -> Astro {
            return Astro(
                moonIllumination: moonIllumination ?? self.moonIllumination,
                moonPhase: moonPhase ?? self.moonPhase,
                moonrise: moonrise ?? self.moonrise,
                moonset: moonset ?? self.moonset,
                sunrise: sunrise ?? self.sunrise,
                sunset: sunset ?? self.sunset
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Day
    public struct Day: Codable, Equatable {
        public let avghumidity, avgtempC, avgtempF, avgvisKM: Double
        public let avgvisMiles: Double
        public let condition: DayCondition
        public let dailyChanceOfRain, dailyChanceOfSnow: Double?
        public let maxtempC, maxtempF, maxwindKph, maxwindMph: Double
        public let mintempC, mintempF, totalprecipIn, totalprecipMm: Double
        public let totalsnowCM, uv: Double

        public enum CodingKeys: String, CodingKey {
            case avghumidity
            case avgtempC = "avgtemp_c"
            case avgtempF = "avgtemp_f"
            case avgvisKM = "avgvis_km"
            case avgvisMiles = "avgvis_miles"
            case condition
            case dailyChanceOfRain = "daily_chance_of_rain"
            case dailyChanceOfSnow = "daily_chance_of_snow"
            case maxtempC = "maxtemp_c"
            case maxtempF = "maxtemp_f"
            case maxwindKph = "maxwind_kph"
            case maxwindMph = "maxwind_mph"
            case mintempC = "mintemp_c"
            case mintempF = "mintemp_f"
            case totalprecipIn = "totalprecip_in"
            case totalprecipMm = "totalprecip_mm"
            case totalsnowCM = "totalsnow_cm"
            case uv
        }

        public init(avghumidity: Double, avgtempC: Double, avgtempF: Double, avgvisKM: Double, avgvisMiles: Double, condition: DayCondition, dailyChanceOfRain: Double?, dailyChanceOfSnow: Double?, maxtempC: Double, maxtempF: Double, maxwindKph: Double, maxwindMph: Double, mintempC: Double, mintempF: Double, totalprecipIn: Double, totalprecipMm: Double, totalsnowCM: Double, uv: Double) {
            self.avghumidity = avghumidity
            self.avgtempC = avgtempC
            self.avgtempF = avgtempF
            self.avgvisKM = avgvisKM
            self.avgvisMiles = avgvisMiles
            self.condition = condition
            self.dailyChanceOfRain = dailyChanceOfRain
            self.dailyChanceOfSnow = dailyChanceOfSnow
            self.maxtempC = maxtempC
            self.maxtempF = maxtempF
            self.maxwindKph = maxwindKph
            self.maxwindMph = maxwindMph
            self.mintempC = mintempC
            self.mintempF = mintempF
            self.totalprecipIn = totalprecipIn
            self.totalprecipMm = totalprecipMm
            self.totalsnowCM = totalsnowCM
            self.uv = uv
        }
    }

    // MARK: Day convenience initializers and mutators

    public extension Day {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Day.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            avghumidity: Double? = nil,
            avgtempC: Double? = nil,
            avgtempF: Double? = nil,
            avgvisKM: Double? = nil,
            avgvisMiles: Double? = nil,
            condition: DayCondition? = nil,
            dailyChanceOfRain: Double?? = nil,
            dailyChanceOfSnow: Double?? = nil,
            maxtempC: Double? = nil,
            maxtempF: Double? = nil,
            maxwindKph: Double? = nil,
            maxwindMph: Double? = nil,
            mintempC: Double? = nil,
            mintempF: Double? = nil,
            totalprecipIn: Double? = nil,
            totalprecipMm: Double? = nil,
            totalsnowCM: Double? = nil,
            uv: Double? = nil
        ) -> Day {
            return Day(
                avghumidity: avghumidity ?? self.avghumidity,
                avgtempC: avgtempC ?? self.avgtempC,
                avgtempF: avgtempF ?? self.avgtempF,
                avgvisKM: avgvisKM ?? self.avgvisKM,
                avgvisMiles: avgvisMiles ?? self.avgvisMiles,
                condition: condition ?? self.condition,
                dailyChanceOfRain: dailyChanceOfRain ?? self.dailyChanceOfRain,
                dailyChanceOfSnow: dailyChanceOfSnow ?? self.dailyChanceOfSnow,
                maxtempC: maxtempC ?? self.maxtempC,
                maxtempF: maxtempF ?? self.maxtempF,
                maxwindKph: maxwindKph ?? self.maxwindKph,
                maxwindMph: maxwindMph ?? self.maxwindMph,
                mintempC: mintempC ?? self.mintempC,
                mintempF: mintempF ?? self.mintempF,
                totalprecipIn: totalprecipIn ?? self.totalprecipIn,
                totalprecipMm: totalprecipMm ?? self.totalprecipMm,
                totalsnowCM: totalsnowCM ?? self.totalsnowCM,
                uv: uv ?? self.uv
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - DayCondition
    public struct DayCondition: Codable, Equatable {
        public let code: Double
        public let icon, text: String

        public init(code: Double, icon: String, text: String) {
            self.code = code
            self.icon = icon
            self.text = text
        }
    }

    // MARK: DayCondition convenience initializers and mutators

    public extension DayCondition {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(DayCondition.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            code: Double? = nil,
            icon: String? = nil,
            text: String? = nil
        ) -> DayCondition {
            return DayCondition(
                code: code ?? self.code,
                icon: icon ?? self.icon,
                text: text ?? self.text
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - Hour
    public struct Hour: Codable, Equatable {
        public let airQuality: HourAirQuality?
        public let chanceOfRain, chanceOfSnow: Double?
        public let cloud: Double
        public let condition: HourCondition
        public let dewpointC, dewpointF, diffRAD, dni: Double?
        public let feelslikeC, feelslikeF: Double
        public let gti, gustKph, gustMph, heatindexC: Double?
        public let heatindexF: Double?
        public let humidity, isDay, precipIn, precipMm: Double
        public let pressureIn, pressureMB: Double
        public let shortRAD, snowCM: Double?
        public let tempC, tempF: Double
        public let time: String
        public let timeEpoch, uv, visKM, visMiles: Double
        public let willItRain, willItSnow: Double?
        public let windDegree: Double
        public let windDir: String
        public let windKph, windMph: Double
        public let windchillC, windchillF: Double?

        public enum CodingKeys: String, CodingKey {
            case airQuality = "air_quality"
            case chanceOfRain = "chance_of_rain"
            case chanceOfSnow = "chance_of_snow"
            case cloud, condition
            case dewpointC = "dewpoint_c"
            case dewpointF = "dewpoint_f"
            case diffRAD = "diff_rad"
            case dni
            case feelslikeC = "feelslike_c"
            case feelslikeF = "feelslike_f"
            case gti
            case gustKph = "gust_kph"
            case gustMph = "gust_mph"
            case heatindexC = "heatindex_c"
            case heatindexF = "heatindex_f"
            case humidity
            case isDay = "is_day"
            case precipIn = "precip_in"
            case precipMm = "precip_mm"
            case pressureIn = "pressure_in"
            case pressureMB = "pressure_mb"
            case shortRAD = "short_rad"
            case snowCM = "snow_cm"
            case tempC = "temp_c"
            case tempF = "temp_f"
            case time
            case timeEpoch = "time_epoch"
            case uv
            case visKM = "vis_km"
            case visMiles = "vis_miles"
            case willItRain = "will_it_rain"
            case willItSnow = "will_it_snow"
            case windDegree = "wind_degree"
            case windDir = "wind_dir"
            case windKph = "wind_kph"
            case windMph = "wind_mph"
            case windchillC = "windchill_c"
            case windchillF = "windchill_f"
        }

        public init(airQuality: HourAirQuality?, chanceOfRain: Double?, chanceOfSnow: Double?, cloud: Double, condition: HourCondition, dewpointC: Double?, dewpointF: Double?, diffRAD: Double?, dni: Double?, feelslikeC: Double, feelslikeF: Double, gti: Double?, gustKph: Double?, gustMph: Double?, heatindexC: Double?, heatindexF: Double?, humidity: Double, isDay: Double, precipIn: Double, precipMm: Double, pressureIn: Double, pressureMB: Double, shortRAD: Double?, snowCM: Double?, tempC: Double, tempF: Double, time: String, timeEpoch: Double, uv: Double, visKM: Double, visMiles: Double, willItRain: Double?, willItSnow: Double?, windDegree: Double, windDir: String, windKph: Double, windMph: Double, windchillC: Double?, windchillF: Double?) {
            self.airQuality = airQuality
            self.chanceOfRain = chanceOfRain
            self.chanceOfSnow = chanceOfSnow
            self.cloud = cloud
            self.condition = condition
            self.dewpointC = dewpointC
            self.dewpointF = dewpointF
            self.diffRAD = diffRAD
            self.dni = dni
            self.feelslikeC = feelslikeC
            self.feelslikeF = feelslikeF
            self.gti = gti
            self.gustKph = gustKph
            self.gustMph = gustMph
            self.heatindexC = heatindexC
            self.heatindexF = heatindexF
            self.humidity = humidity
            self.isDay = isDay
            self.precipIn = precipIn
            self.precipMm = precipMm
            self.pressureIn = pressureIn
            self.pressureMB = pressureMB
            self.shortRAD = shortRAD
            self.snowCM = snowCM
            self.tempC = tempC
            self.tempF = tempF
            self.time = time
            self.timeEpoch = timeEpoch
            self.uv = uv
            self.visKM = visKM
            self.visMiles = visMiles
            self.willItRain = willItRain
            self.willItSnow = willItSnow
            self.windDegree = windDegree
            self.windDir = windDir
            self.windKph = windKph
            self.windMph = windMph
            self.windchillC = windchillC
            self.windchillF = windchillF
        }
    }

    // MARK: Hour convenience initializers and mutators

    public extension Hour {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(Hour.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            airQuality: HourAirQuality?? = nil,
            chanceOfRain: Double?? = nil,
            chanceOfSnow: Double?? = nil,
            cloud: Double? = nil,
            condition: HourCondition? = nil,
            dewpointC: Double?? = nil,
            dewpointF: Double?? = nil,
            diffRAD: Double?? = nil,
            dni: Double?? = nil,
            feelslikeC: Double? = nil,
            feelslikeF: Double? = nil,
            gti: Double?? = nil,
            gustKph: Double?? = nil,
            gustMph: Double?? = nil,
            heatindexC: Double?? = nil,
            heatindexF: Double?? = nil,
            humidity: Double? = nil,
            isDay: Double? = nil,
            precipIn: Double? = nil,
            precipMm: Double? = nil,
            pressureIn: Double? = nil,
            pressureMB: Double? = nil,
            shortRAD: Double?? = nil,
            snowCM: Double?? = nil,
            tempC: Double? = nil,
            tempF: Double? = nil,
            time: String? = nil,
            timeEpoch: Double? = nil,
            uv: Double? = nil,
            visKM: Double? = nil,
            visMiles: Double? = nil,
            willItRain: Double?? = nil,
            willItSnow: Double?? = nil,
            windDegree: Double? = nil,
            windDir: String? = nil,
            windKph: Double? = nil,
            windMph: Double? = nil,
            windchillC: Double?? = nil,
            windchillF: Double?? = nil
        ) -> Hour {
            return Hour(
                airQuality: airQuality ?? self.airQuality,
                chanceOfRain: chanceOfRain ?? self.chanceOfRain,
                chanceOfSnow: chanceOfSnow ?? self.chanceOfSnow,
                cloud: cloud ?? self.cloud,
                condition: condition ?? self.condition,
                dewpointC: dewpointC ?? self.dewpointC,
                dewpointF: dewpointF ?? self.dewpointF,
                diffRAD: diffRAD ?? self.diffRAD,
                dni: dni ?? self.dni,
                feelslikeC: feelslikeC ?? self.feelslikeC,
                feelslikeF: feelslikeF ?? self.feelslikeF,
                gti: gti ?? self.gti,
                gustKph: gustKph ?? self.gustKph,
                gustMph: gustMph ?? self.gustMph,
                heatindexC: heatindexC ?? self.heatindexC,
                heatindexF: heatindexF ?? self.heatindexF,
                humidity: humidity ?? self.humidity,
                isDay: isDay ?? self.isDay,
                precipIn: precipIn ?? self.precipIn,
                precipMm: precipMm ?? self.precipMm,
                pressureIn: pressureIn ?? self.pressureIn,
                pressureMB: pressureMB ?? self.pressureMB,
                shortRAD: shortRAD ?? self.shortRAD,
                snowCM: snowCM ?? self.snowCM,
                tempC: tempC ?? self.tempC,
                tempF: tempF ?? self.tempF,
                time: time ?? self.time,
                timeEpoch: timeEpoch ?? self.timeEpoch,
                uv: uv ?? self.uv,
                visKM: visKM ?? self.visKM,
                visMiles: visMiles ?? self.visMiles,
                willItRain: willItRain ?? self.willItRain,
                willItSnow: willItSnow ?? self.willItSnow,
                windDegree: windDegree ?? self.windDegree,
                windDir: windDir ?? self.windDir,
                windKph: windKph ?? self.windKph,
                windMph: windMph ?? self.windMph,
                windchillC: windchillC ?? self.windchillC,
                windchillF: windchillF ?? self.windchillF
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - HourAirQuality
    public struct HourAirQuality: Codable, Equatable {
        public let co, gbDefraIndex, no2, o3: Double
        public let pm10, pm25, so2, usEpaIndex: Double

        public enum CodingKeys: String, CodingKey {
            case co
            case gbDefraIndex = "gb-defra-index"
            case no2, o3, pm10
            case pm25 = "pm2_5"
            case so2
            case usEpaIndex = "us-epa-index"
        }

        public init(co: Double, gbDefraIndex: Double, no2: Double, o3: Double, pm10: Double, pm25: Double, so2: Double, usEpaIndex: Double) {
            self.co = co
            self.gbDefraIndex = gbDefraIndex
            self.no2 = no2
            self.o3 = o3
            self.pm10 = pm10
            self.pm25 = pm25
            self.so2 = so2
            self.usEpaIndex = usEpaIndex
        }
    }

    // MARK: HourAirQuality convenience initializers and mutators

    public extension HourAirQuality {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(HourAirQuality.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            co: Double? = nil,
            gbDefraIndex: Double? = nil,
            no2: Double? = nil,
            o3: Double? = nil,
            pm10: Double? = nil,
            pm25: Double? = nil,
            so2: Double? = nil,
            usEpaIndex: Double? = nil
        ) -> HourAirQuality {
            return HourAirQuality(
                co: co ?? self.co,
                gbDefraIndex: gbDefraIndex ?? self.gbDefraIndex,
                no2: no2 ?? self.no2,
                o3: o3 ?? self.o3,
                pm10: pm10 ?? self.pm10,
                pm25: pm25 ?? self.pm25,
                so2: so2 ?? self.so2,
                usEpaIndex: usEpaIndex ?? self.usEpaIndex
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - HourCondition
    public struct HourCondition: Codable, Equatable {
        public let code: Double
        public let icon, text: String

        public init(code: Double, icon: String, text: String) {
            self.code = code
            self.icon = icon
            self.text = text
        }
    }

    // MARK: HourCondition convenience initializers and mutators

    public extension HourCondition {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(HourCondition.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            code: Double? = nil,
            icon: String? = nil,
            text: String? = nil
        ) -> HourCondition {
            return HourCondition(
                code: code ?? self.code,
                icon: icon ?? self.icon,
                text: text ?? self.text
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - WeatherForecastResponseLocation
    public struct WeatherForecastResponseLocation: Codable, Equatable {
        public let country: String
        public let id, lat: Double
        public let localtime: String?
        public let localtimeEpoch: Double?
        public let lon: Double
        public let name, region: String
        public let tzID: String?

        public enum CodingKeys: String, CodingKey {
            case country, id, lat, localtime
            case localtimeEpoch = "localtime_epoch"
            case lon, name, region
            case tzID = "tz_id"
        }

        public init(country: String, id: Double, lat: Double, localtime: String?, localtimeEpoch: Double?, lon: Double, name: String, region: String, tzID: String?) {
            self.country = country
            self.id = id
            self.lat = lat
            self.localtime = localtime
            self.localtimeEpoch = localtimeEpoch
            self.lon = lon
            self.name = name
            self.region = region
            self.tzID = tzID
        }
    }

    // MARK: WeatherForecastResponseLocation convenience initializers and mutators

    public extension WeatherForecastResponseLocation {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(WeatherForecastResponseLocation.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            country: String? = nil,
            id: Double? = nil,
            lat: Double? = nil,
            localtime: String?? = nil,
            localtimeEpoch: Double?? = nil,
            lon: Double? = nil,
            name: String? = nil,
            region: String? = nil,
            tzID: String?? = nil
        ) -> WeatherForecastResponseLocation {
            return WeatherForecastResponseLocation(
                country: country ?? self.country,
                id: id ?? self.id,
                lat: lat ?? self.lat,
                localtime: localtime ?? self.localtime,
                localtimeEpoch: localtimeEpoch ?? self.localtimeEpoch,
                lon: lon ?? self.lon,
                name: name ?? self.name,
                region: region ?? self.region,
                tzID: tzID ?? self.tzID
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    //
    // Hashable or Equatable:
    // The compiler will not be able to synthesize the implementation of Hashable or Equatable
    // for types that require the use of JSONAny, nor will the implementation of Hashable be
    // synthesized for types that have collections (such as arrays or dictionaries).

    // MARK: - WildlifeWildlifeIdentifyRequest
    public struct WildlifeWildlifeIdentifyRequest: Codable, Equatable {
        /// Uploaded image key in R2
        public let image: String

        public init(image: String) {
            self.image = image
        }
    }

    // MARK: WildlifeWildlifeIdentifyRequest convenience initializers and mutators

    public extension WildlifeWildlifeIdentifyRequest {
        init(data: Data) throws {
            self = try newJSONDecoder().decode(WildlifeWildlifeIdentifyRequest.self, from: data)
        }

        init(_ json: String, using encoding: String.Encoding = .utf8) throws {
            guard let data = json.data(using: encoding) else {
                throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
            }
            try self.init(data: data)
        }

        init(fromURL url: URL) throws {
            try self.init(data: try Data(contentsOf: url))
        }

        func with(
            image: String? = nil
        ) -> WildlifeWildlifeIdentifyRequest {
            return WildlifeWildlifeIdentifyRequest(
                image: image ?? self.image
            )
        }

        func jsonData() throws -> Data {
            return try newJSONEncoder().encode(self)
        }

        func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
            return String(data: try self.jsonData(), encoding: encoding)
        }
    }

    // MARK: - Helper functions for creating encoders and decoders

    func newJSONDecoder() -> JSONDecoder {
        let decoder = JSONDecoder()
        if #available(iOS 10.0, OSX 10.12, tvOS 10.0, watchOS 3.0, *) {
            decoder.dateDecodingStrategy = .iso8601
        }
        return decoder
    }

    func newJSONEncoder() -> JSONEncoder {
        let encoder = JSONEncoder()
        if #available(iOS 10.0, OSX 10.12, tvOS 10.0, watchOS 3.0, *) {
            encoder.dateEncodingStrategy = .iso8601
        }
        return encoder
    }

    // MARK: - Encode/decode helpers

    public class JSONNull: Codable, Hashable {

        public static func == (lhs: JSONNull, rhs: JSONNull) -> Bool {
                return true
        }

        public var hashValue: Int {
                return 0
        }

        public init() {}

        public required init(from decoder: Decoder) throws {
                let container = try decoder.singleValueContainer()
                if !container.decodeNil() {
                        throw DecodingError.typeMismatch(JSONNull.self, DecodingError.Context(codingPath: decoder.codingPath, debugDescription: "Wrong type for JSONNull"))
                }
        }

        public func encode(to encoder: Encoder) throws {
                var container = encoder.singleValueContainer()
                try container.encodeNil()
        }
    }

    class JSONCodingKey: CodingKey {
        let key: String

        required init?(intValue: Int) {
                return nil
        }

        required init?(stringValue: String) {
                key = stringValue
        }

        var intValue: Int? {
                return nil
        }

        var stringValue: String {
                return key
        }
    }

    public class JSONAny: Codable {

        public let value: Any

        static func decodingError(forCodingPath codingPath: [CodingKey]) -> DecodingError {
                let context = DecodingError.Context(codingPath: codingPath, debugDescription: "Cannot decode JSONAny")
                return DecodingError.typeMismatch(JSONAny.self, context)
        }

        static func encodingError(forValue value: Any, codingPath: [CodingKey]) -> EncodingError {
                let context = EncodingError.Context(codingPath: codingPath, debugDescription: "Cannot encode JSONAny")
                return EncodingError.invalidValue(value, context)
        }

        static func decode(from container: SingleValueDecodingContainer) throws -> Any {
                if let value = try? container.decode(Bool.self) {
                        return value
                }
                if let value = try? container.decode(Int64.self) {
                        return value
                }
                if let value = try? container.decode(Double.self) {
                        return value
                }
                if let value = try? container.decode(String.self) {
                        return value
                }
                if container.decodeNil() {
                        return JSONNull()
                }
                throw decodingError(forCodingPath: container.codingPath)
        }

        static func decode(from container: inout UnkeyedDecodingContainer) throws -> Any {
                if let value = try? container.decode(Bool.self) {
                        return value
                }
                if let value = try? container.decode(Int64.self) {
                        return value
                }
                if let value = try? container.decode(Double.self) {
                        return value
                }
                if let value = try? container.decode(String.self) {
                        return value
                }
                if let value = try? container.decodeNil() {
                        if value {
                                return JSONNull()
                        }
                }
                if var container = try? container.nestedUnkeyedContainer() {
                        return try decodeArray(from: &container)
                }
                if var container = try? container.nestedContainer(keyedBy: JSONCodingKey.self) {
                        return try decodeDictionary(from: &container)
                }
                throw decodingError(forCodingPath: container.codingPath)
        }

        static func decode(from container: inout KeyedDecodingContainer<JSONCodingKey>, forKey key: JSONCodingKey) throws -> Any {
                if let value = try? container.decode(Bool.self, forKey: key) {
                        return value
                }
                if let value = try? container.decode(Int64.self, forKey: key) {
                        return value
                }
                if let value = try? container.decode(Double.self, forKey: key) {
                        return value
                }
                if let value = try? container.decode(String.self, forKey: key) {
                        return value
                }
                if let value = try? container.decodeNil(forKey: key) {
                        if value {
                                return JSONNull()
                        }
                }
                if var container = try? container.nestedUnkeyedContainer(forKey: key) {
                        return try decodeArray(from: &container)
                }
                if var container = try? container.nestedContainer(keyedBy: JSONCodingKey.self, forKey: key) {
                        return try decodeDictionary(from: &container)
                }
                throw decodingError(forCodingPath: container.codingPath)
        }

        static func decodeArray(from container: inout UnkeyedDecodingContainer) throws -> [Any] {
                var arr: [Any] = []
                while !container.isAtEnd {
                        let value = try decode(from: &container)
                        arr.append(value)
                }
                return arr
        }

        static func decodeDictionary(from container: inout KeyedDecodingContainer<JSONCodingKey>) throws -> [String: Any] {
                var dict = [String: Any]()
                for key in container.allKeys {
                        let value = try decode(from: &container, forKey: key)
                        dict[key.stringValue] = value
                }
                return dict
        }

        static func encode(to container: inout UnkeyedEncodingContainer, array: [Any]) throws {
                for value in array {
                        if let value = value as? Bool {
                                try container.encode(value)
                        } else if let value = value as? Int64 {
                                try container.encode(value)
                        } else if let value = value as? Double {
                                try container.encode(value)
                        } else if let value = value as? String {
                                try container.encode(value)
                        } else if value is JSONNull {
                                try container.encodeNil()
                        } else if let value = value as? [Any] {
                                var container = container.nestedUnkeyedContainer()
                                try encode(to: &container, array: value)
                        } else if let value = value as? [String: Any] {
                                var container = container.nestedContainer(keyedBy: JSONCodingKey.self)
                                try encode(to: &container, dictionary: value)
                        } else {
                                throw encodingError(forValue: value, codingPath: container.codingPath)
                        }
                }
        }

        static func encode(to container: inout KeyedEncodingContainer<JSONCodingKey>, dictionary: [String: Any]) throws {
                for (key, value) in dictionary {
                        let key = JSONCodingKey(stringValue: key)!
                        if let value = value as? Bool {
                                try container.encode(value, forKey: key)
                        } else if let value = value as? Int64 {
                                try container.encode(value, forKey: key)
                        } else if let value = value as? Double {
                                try container.encode(value, forKey: key)
                        } else if let value = value as? String {
                                try container.encode(value, forKey: key)
                        } else if value is JSONNull {
                                try container.encodeNil(forKey: key)
                        } else if let value = value as? [Any] {
                                var container = container.nestedUnkeyedContainer(forKey: key)
                                try encode(to: &container, array: value)
                        } else if let value = value as? [String: Any] {
                                var container = container.nestedContainer(keyedBy: JSONCodingKey.self, forKey: key)
                                try encode(to: &container, dictionary: value)
                        } else {
                                throw encodingError(forValue: value, codingPath: container.codingPath)
                        }
                }
        }

        static func encode(to container: inout SingleValueEncodingContainer, value: Any) throws {
                if let value = value as? Bool {
                        try container.encode(value)
                } else if let value = value as? Int64 {
                        try container.encode(value)
                } else if let value = value as? Double {
                        try container.encode(value)
                } else if let value = value as? String {
                        try container.encode(value)
                } else if value is JSONNull {
                        try container.encodeNil()
                } else {
                        throw encodingError(forValue: value, codingPath: container.codingPath)
                }
        }

        public required init(from decoder: Decoder) throws {
                if var arrayContainer = try? decoder.unkeyedContainer() {
                        self.value = try JSONAny.decodeArray(from: &arrayContainer)
                } else if var container = try? decoder.container(keyedBy: JSONCodingKey.self) {
                        self.value = try JSONAny.decodeDictionary(from: &container)
                } else {
                        let container = try decoder.singleValueContainer()
                        self.value = try JSONAny.decode(from: container)
                }
        }

        public func encode(to encoder: Encoder) throws {
                if let arr = self.value as? [Any] {
                        var container = encoder.unkeyedContainer()
                        try JSONAny.encode(to: &container, array: arr)
                } else if let dict = self.value as? [String: Any] {
                        var container = encoder.container(keyedBy: JSONCodingKey.self)
                        try JSONAny.encode(to: &container, dictionary: dict)
                } else {
                        var container = encoder.singleValueContainer()
                        try JSONAny.encode(to: &container, value: self.value)
                }
        }
    }

}
