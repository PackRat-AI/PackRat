pre-push:
  parallel: true
  commands:
    format:
      run: bun format
      skip:
        - merge
        - ref: main
        - env: CI

    # Ready for when you add linting
    # lint:
    #   run: bun lint
